# 8편: SEMO v5.0 - 구조의 대수술

> 시리즈: AI와 함께 일하는 법을 만들다 (8편, 특별편)

---

## 프롤로그: "너무 복잡해졌어"

2024년 12월 말, SEMO는 11개의 패키지로 성장했다.

```
packages/
├── biz/          # 비즈니스 (discovery, design, management, poc)
├── eng/          # 엔지니어링 (nextjs, spring, ms, infra)
├── ops/          # 운영 (qa, monitor)
├── core/         # 코어 원칙
├── meta/         # 메타 패키지
├── cli/          # CLI 도구
└── mcp-server/   # MCP 통합
```

하지만 이 구조에는 심각한 문제가 있었다.

> **"어떤 스킬이 어디에 있는지 모르겠어요."**

---

## 문제 1: 분산된 스킬

### 같은 기능, 다른 위치

```
implement 스킬:
├── packages/core/skills/implement/      # 기본
├── packages/eng/nextjs/skills/implement/ # Next.js 특화
├── packages/eng/spring/skills/implement/ # Spring 특화
└── packages/biz/poc/skills/implement-mvp/ # PoC 특화
```

AI가 어떤 implement를 호출해야 할지 헷갈렸다.

### 중복 코드

```markdown
# packages/core/skills/tester/SKILL.md
"테스트 작성 및 실행"

# packages/eng/nextjs/skills/tester/SKILL.md
"테스트 작성 및 실행 (Next.js 특화)"
# → 90%가 core와 동일
```

---

## 문제 2: 레거시의 늪

### 사용하지 않는 파일들

```
packages/core/
├── evaluation/     # Promptfoo 평가 (미사용)
├── observability/  # Langfuse 설정 (미사용)
├── rag/            # RAG 인덱스 (미사용)
├── security/       # 보안 감사 (미사용)
└── workflows/      # 승인 게이트 (미사용)
```

11개월 동안 쌓인 "나중에 쓸 것 같은" 파일들.

### 버전 불일치

```bash
$ cat packages/core/VERSION
0.33.0

$ cat semo-system/semo-core/VERSION
2.1.0
```

두 개의 "core"가 존재했다.

---

## 해결책: v5.0 대수술

### Phase 1: semo-system 통합

모든 Standard 패키지를 `semo-system/` 아래로 통합.

```
semo-system/
├── semo-core/       # 원칙, 오케스트레이터, 커맨드
├── semo-skills/     # 14개 통합 스킬
├── semo-agents/     # 14개 페르소나 에이전트 (신규!)
├── semo-scripts/    # 유틸리티 스크립트 (신규!)
├── semo-hooks/      # Git hooks
├── semo-remote/     # 원격 작업 관리
└── meta/            # 메타 패키지
```

### Phase 2: 스킬 통합

분산된 스킬들을 `semo-skills/` 하나로 통합.

```
semo-skills/ (14개)
├── implement/       # 코드 작성 (플랫폼 자동 감지)
├── tester/          # 테스트 작성/실행
├── planner/         # 계획 수립
├── deployer/        # 배포
├── git-workflow/    # Git 작업
├── memory/          # 컨텍스트 관리
├── notify-slack/    # Slack 알림
├── feedback/        # 피드백 수집
├── version-updater/ # 버전 관리
├── semo-help/       # 도움말
├── semo-architecture-checker/ # 구조 검증
├── circuit-breaker/ # 무한 루프 방지
├── list-bugs/       # 버그 목록
└── project-status/  # 프로젝트 상태
```

### Phase 3: 페르소나 에이전트 도입

BMad Method에서 영감을 받은 14개의 페르소나 기반 에이전트.

```
semo-agents/
├── planning/
│   ├── po.md       # Product Owner (John)
│   ├── pm.md       # Project Manager (Bob)
│   └── analyst.md  # Business Analyst (Mary)
├── architecture/
│   ├── architect.md  # Software Architect (Winston)
│   ├── designer.md   # UX Designer (Sally)
│   └── dba.md        # DB Architect (Derek)
├── development/
│   ├── developer.md  # Senior Developer (Dave)
│   ├── reviewer.md   # Code Reviewer (Rachel)
│   └── tech-writer.md # Technical Writer (Wendy)
├── quality/
│   ├── qa.md         # QA Engineer (Murat)
│   └── security.md   # Security Engineer (Sam)
└── operations/
    ├── devops.md     # DevOps Engineer (Dan)
    ├── sre.md        # SRE (Stella)
    └── release.md    # Release Manager (Ryan)
```

#### Party Mode: 다관점 토론

```markdown
사용자: "금요일 오후에 배포할까요?"

📦 **Release Manager (Ryan)**:
"Don't deploy on Friday" - 이건 업계 상식입니다.
월요일 오전 첫 배포로 미루거나, 오늘 오전 중 배포합시다.

📊 **SRE (Stella)**:
현재 에러 버짓의 80%를 이미 소진했습니다.
카나리 배포로 10% 트래픽에만 먼저 적용하고 모니터링하죠.

🏗️ **PO (John)**:
비즈니스 관점에서 월요일 배포도 괜찮습니다.
단, 고객사 미팅이 화요일이니 그 전에 완료해야 해요.
```

### Phase 4: 레거시 정리

```bash
# 삭제된 디렉토리
rm -rf packages/core/     # → semo-system/semo-core/
rm -rf packages/meta/     # → semo-system/meta/
rm -rf packages/biz/      # → semo-skills/ 통합
rm -rf packages/eng/      # → semo-skills/ 통합
rm -rf packages/ops/      # → semo-skills/ 통합

# 남은 패키지 (npm 배포용만)
packages/
├── cli/          # @team-semicolon/semo-cli
└── mcp-server/   # @team-semicolon/semo-mcp
```

---

## 주요 변경사항 타임라인

### v4.0 (2024-12-25)

```
feat(core): SEMO v4.0 - semo-core 단일 패키지 통합 (2.0.0)
- 분산된 스킬을 semo-core/skills/로 통합
- 152개 확장 스킬 병합
- 라우팅 테이블 중앙 집중화
```

### v5.0 Phase 1 (2024-12-29)

```
feat(agents,scripts): SEMO v5.0 Phase 1 - 페르소나 기반 Agent 시스템
- semo-agents 1.0.0: 14개 페르소나 에이전트
- semo-scripts 1.0.0: 유틸리티 스크립트 패키지
```

### v5.0 Phase 2-4 (2024-12-29)

```
feat(core): /SEMO:health 커맨드 추가 (2.2.0)
feat(scripts): semo-scripts 유틸리티 스크립트 구현 (1.1.0)
refactor(packages): 레거시 packages/core, packages/meta 삭제 (v5.0.0)
```

---

## 숫자로 보는 변화

| 지표 | Before (v3.0) | After (v5.0) | 변화 |
|------|--------------|--------------|------|
| 패키지 수 | 11개 | 7개 | -36% |
| 스킬 파일 수 | 152개 (분산) | 14개 (통합) | 단순화 |
| 루트 디렉토리 | 복잡 | 깔끔 | 정리 |
| 중복 코드 | 많음 | 없음 | 제거 |
| npm 배포 패키지 | 2개 | 2개 | 유지 |

---

## CLI 개선사항

### v3.2.0: 레거시 마이그레이션

```bash
# 레거시 환경 자동 감지
$ semo migrate
✅ 레거시 환경이 감지되지 않았습니다.

# 레거시 환경인 경우
$ semo migrate
⚠️  구버전 SEMO 구조 감지됨
→ 마이그레이션 가이드 출력
```

### v3.3.0: 새 구조 지원

```bash
$ semo version

📦 SEMO 버전 정보

┌────────────────┬──────────┬──────────┬────────┐
│ 패키지         │ 설치됨   │ 최신     │ 상태   │
├────────────────┼──────────┼──────────┼────────┤
│ semo-cli       │ 3.3.0    │ 3.3.0    │ ✓ 최신 │
│ semo-core      │ 2.2.0    │ 2.2.0    │ ✓ 최신 │
│ semo-skills    │ 1.13.0   │ 1.13.0   │ ✓ 최신 │
│ semo-agents    │ 1.0.0    │ 1.0.0    │ ✓ 최신 │
│ semo-scripts   │ 1.1.0    │ 1.1.0    │ ✓ 최신 │
└────────────────┴──────────┴──────────┴────────┘
```

---

## 새로운 커맨드

### /SEMO:health

```markdown
사용자: /SEMO:health

[SEMO] Skill: semo-architecture-checker 호출

## .claude 디렉토리 검증 결과

| 항목 | 상태 | 비고 |
|------|------|------|
| semo-core | ✅ | 존재 |
| semo-skills | ✅ | 14 symlinks |
| agents/ | ✅ | 6 symlinks |
| CLAUDE.md | ✅ | 심링크 유효 |

**결과**: ✅ 구조 정상
```

---

## 스크립트 유틸리티

`semo-scripts` 패키지에 추가된 자동화 스크립트들:

```bash
# 구조 검증
./semo-system/semo-scripts/health/validate-structure.sh

# 버전 확인
./semo-system/semo-scripts/health/check-versions.sh

# 새 스킬 생성
./semo-system/semo-scripts/scaffold/create-skill.sh my-skill

# 새 에이전트 생성
./semo-system/semo-scripts/scaffold/create-agent.sh my-agent "Alex"

# 패키지 검증
./semo-system/semo-scripts/deploy/validate-package.sh ./semo-system/semo-core

# 버전 자동 증가
./semo-system/semo-scripts/deploy/bump-version.sh ./semo-system/semo-core minor
```

---

## 마이그레이션 가이드

### 기존 사용자

```bash
# 1. CLI 업데이트
npm update -g @team-semicolon/semo-cli

# 2. 기존 .claude 삭제
rm -rf .claude

# 3. 새로 설치
semo init
```

### 레거시 환경 감지

CLI가 자동으로 레거시 환경을 감지하고 가이드를 제공합니다:

```bash
$ semo version

⚠️  구버전 SEMO 구조가 감지되었습니다.
   루트에 semo-core/ 또는 semo-skills/가 있습니다.

💡 마이그레이션 방법:
   1. 기존 semo-core/, semo-skills/ 폴더 삭제
   2. .claude/ 폴더 삭제
   3. semo init 다시 실행
```

---

## 교훈

### 1. 복잡성은 점진적으로 쌓인다

처음에는 합리적이었던 11개 패키지 구조가, 시간이 지나면서 관리 불가능해졌다.

### 2. 정리는 빠를수록 좋다

"나중에 정리하자"는 말은 "영원히 안 하겠다"와 같다.

### 3. Breaking Change를 두려워하지 마라

v5.0은 대규모 Breaking Change였지만, 결과적으로 더 나은 구조를 만들었다.

### 4. 자동화가 핵심이다

CLI의 레거시 감지, 마이그레이션 가이드가 없었다면 사용자들이 혼란스러웠을 것이다.

---

## 다음 단계

### v5.1 (2025 Q1)

- 벡터 임베딩 기반 스마트 검색
- Party Mode 고도화
- 멀티 에이전트 협업 프로토타입

### v5.2 (2025 Q2)

- 실시간 협업 메모리
- 프로젝트 간 지식 공유
- 성능 대시보드

---

## 마무리

v5.0은 "대수술"이었다. 11개의 패키지를 7개로 줄이고, 152개의 분산된 스킬을 14개로 통합했다.

하지만 이건 끝이 아니라 새로운 시작이다.

더 단순하고, 더 강력하고, 더 확장 가능한 SEMO.

이제 진짜 AI 오케스트레이션이 시작된다.

---

*이전 편: [AI 에이전트가 기억하는 세상](./07-future-memory.md)*
*시리즈 목차: [AI와 함께 일하는 법을 만들다](./00-series-index.md)*
