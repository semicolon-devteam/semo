# E8 T8.2 건기식 광고규제(식약처) 가드레일 — PoC 결과 (2026-06-09)

**목적**: 건강기능식품 표시·광고 규정 위반(질병 예방·치료 효능, 의약품 오인, 거짓·과장)을 콘텐츠 생성 산출물에서 검출·교정. 김민식 건기식 파일럿의 법적 선결. 외부계정 불요.

## 결과 (PASS)

2단 검사(규칙 denylist + LLM 분류) — 실 LLM(gpt-4o-mini):

- **위반 샘플**("…감기를 예방하고…혈압을 낮추고 암을 막아줍니다. 부작용 없는 특효…") → denylist 5종 히트 + LLM **4건 위반 검출**(질병치료×3·거짓과장×1) + 각 **규정준수 리라이트**("건강한 면역 기능 유지에 도움을 줄 수 있습니다" 등).
- **정상 샘플**("건강한 일상에 도움을 줄 수 있습니다") → **오탐 0**(clean).

## 의미 / 적용

- E1 콘텐츠 생성(특히 건기식 테넌트)에 **출력 게이트**로 삽입: 생성 → 가드 검사 → 위반 시 리라이트/차단. 전 모듈 공통 안전규칙(단정금지)과 함께 동작.
- 잔여: 기능성 인정범위(식약처 고시) 화이트리스트 정교화, 업종 플래그(tenant=건기식)일 때 자동 적용 배선.

## 산출물

`docs/reports/poc/e8-healthfood-ad-guard-poc.mjs`.
