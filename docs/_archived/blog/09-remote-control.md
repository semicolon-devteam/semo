# 9편: 원격 제어 - 모바일에서 Claude Code를 다루다

> 시리즈: AI와 함께 일하는 법을 만들다 (9/10)

---

## 프롤로그: "회의 중인데 빌드 결과 확인해야 하는데..."

2025년 1월, SEMO v5.0 안정화 후.

"회의 중인데 CI 빌드 결과 확인해야 해요."
"노트북 열기 애매한데 Claude가 뭐하고 있는지 보고 싶어요."
"핸드폰으로 Claude Code 세션에 명령 내릴 수 없나요?"

개발자의 하루는 노트북 앞에서만 이루어지지 않는다. 회의실, 카페, 이동 중... 하지만 Claude Code는 터미널 앞에 앉아 있어야만 쓸 수 있었다.

> **"핸드폰으로 Claude를 조종할 수 없을까?"**

---

## SEMO Remote의 시작

### 비전: 어디서든 Claude Code

```
Before:
[개발자] ← 노트북 필수 → [Claude Code]

After:
[개발자] ← 모바일 PWA → [SEMO Remote] ← → [Claude Code]
```

SEMO Remote는 모바일 PWA에서 Claude Code 세션을 원격으로 제어하는 시스템이다.

### 핵심 구성 요소

| 컴포넌트 | 역할 |
|----------|------|
| **Mobile PWA** | 모바일 웹앱, 명령 전송 |
| **semo-remote-client** | 로컬 Electron 앱, iTerm2 제어 |
| **Supabase Realtime** | 양방향 실시간 통신 |
| **MCP Server** | 토큰/메모리 관리 |

---

## 아키텍처: 실시간 양방향 통신

### 데이터 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                     Mobile PWA (어디서든)                    │
│  "테스트 실행해줘" 버튼 클릭                                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  Supabase PostgreSQL                         │
│                                                              │
│   remote_requests 테이블 (Realtime 활성화)                    │
│   ┌─────────────────────────────────────────────────────┐   │
│   │ session_id: "abc123"                                │   │
│   │ type: "user_question"                               │   │
│   │ message: "테스트 실행해줘"                            │   │
│   │ status: "pending"                                   │   │
│   └─────────────────────────────────────────────────────┘   │
└──────────────────────────┬──────────────────────────────────┘
                           │ Realtime Subscribe
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              semo-remote-client (로컬 Mac)                   │
│                                                              │
│   Realtime 구독 → INSERT 감지 → iTerm2 API 호출             │
│                           │                                  │
│                           ▼                                  │
│   ┌───────────────────────────────────────────────────────┐ │
│   │                  iTerm2 Session                        │ │
│   │                                                        │ │
│   │   $ claude                                            │ │
│   │   Claude Code> [프롬프트 자동 입력]                     │ │
│   │                                                        │ │
│   └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### Supabase Realtime의 마법

```typescript
// Mobile PWA에서 요청 생성
const { data } = await supabase
  .from('remote_requests')
  .insert({
    session_id: currentSessionId,
    type: 'user_question',
    message: '테스트 실행해줘',
    status: 'pending'
  });

// semo-remote-client에서 구독
supabase
  .channel('remote_requests')
  .on('postgres_changes', {
    event: 'INSERT',
    schema: 'public',
    table: 'remote_requests',
    filter: `session_id=eq.${sessionId}`
  }, handleRequest)
  .subscribe();
```

---

## v0.4.0: 테스트 커맨드의 탄생

### 통합 테스트의 필요성

Mobile PWA와 Claude Code 간 통신이 제대로 되는지 어떻게 확인할까?

세 가지 핵심 인터랙션을 테스트하는 커맨드를 만들었다.

### 1. 권한 요청 테스트

```markdown
사용자: /SEMO:permission-test

[SEMO] Remote Test: 권한 요청 테스트 시작

→ 모바일에서 "파일 생성 허용" 버튼 클릭 요청
→ 사용자가 모바일에서 "승인" 버튼 클릭
→ Claude Code가 승인 수신

✅ 권한 요청 테스트 완료
```

**테스트 시나리오:**
1. Claude Code가 파일 생성 권한 요청
2. Mobile PWA에 권한 팝업 표시
3. 사용자가 승인/거부
4. 결과가 Claude Code에 전달

### 2. 옵션 선택 테스트

```markdown
사용자: /SEMO:select-test

[SEMO] Remote Test: 옵션 선택 테스트 시작

모바일에서 아래 옵션 중 선택해주세요:
A. 첫 번째 옵션
B. 두 번째 옵션
C. 세 번째 옵션

→ 사용자가 모바일에서 "B" 선택
→ Claude Code: "두 번째 옵션이 선택되었습니다"

✅ 옵션 선택 테스트 완료
```

**테스트 시나리오:**
1. Claude Code가 AskUserQuestion 호출
2. Mobile PWA에 선택지 UI 표시
3. 사용자가 선택
4. 선택 결과가 Claude Code에 전달

### 3. 텍스트 입력 테스트

```markdown
사용자: /SEMO:input-test

[SEMO] Remote Test: 텍스트 입력 테스트 시작

모바일에서 테스트 메시지를 입력해주세요.

→ 사용자가 모바일에서 "안녕하세요" 입력
→ Claude Code: "입력된 메시지: 안녕하세요"

✅ 텍스트 입력 테스트 완료
```

---

## MCP 도구: semo_remote_request

### 원격 요청 생성

```typescript
// MCP Server에서 제공하는 도구
async function semo_remote_request(params: {
  session_id: string;
  type: 'permission' | 'user_question' | 'text_input' | 'selection';
  message: string;
  options?: string[];
  tool_name?: string;
}): Promise<RemoteResponse> {
  // 1. Supabase에 요청 INSERT
  const { data: request } = await supabase
    .from('remote_requests')
    .insert({
      session_id: params.session_id,
      type: params.type,
      message: params.message,
      options: params.options,
      status: 'pending'
    })
    .select()
    .single();

  // 2. 응답 대기 (Polling 또는 Realtime)
  const response = await waitForResponse(request.id, 60000);

  return response;
}
```

### 원격 응답 처리

```typescript
// Mobile PWA에서 응답
async function semo_remote_respond(params: {
  request_id: string;
  status: 'approved' | 'denied' | 'responded';
  response?: string;
}): Promise<void> {
  await supabase
    .from('remote_requests')
    .update({
      status: params.status,
      response: params.response,
      responded_at: new Date().toISOString()
    })
    .eq('id', params.request_id);
}
```

---

## 실전 시나리오

### 시나리오 1: 회의 중 빌드 승인

```
09:30 - 개발자가 PR 올림
09:35 - CI 빌드 완료, 배포 승인 필요
09:40 - 개발자는 회의 중 (노트북 없음)

[Mobile PWA 알림]
"PR #123 배포 승인이 필요합니다"
[승인] [거부]

→ 개발자가 [승인] 터치
→ Claude Code가 배포 진행
```

### 시나리오 2: 이동 중 코드 수정 요청

```
[Mobile PWA]
사용자: "로그인 버튼 색상 파란색으로 바꿔줘"

→ semo-remote-client가 요청 수신
→ iTerm2의 Claude Code 세션에 프롬프트 전달
→ Claude Code가 코드 수정
→ 결과가 Mobile PWA에 표시
```

### 시나리오 3: 파일 생성 원격 승인

```
Claude Code: "config.json 파일을 생성하려고 합니다"

[Mobile PWA 팝업]
"Claude Code가 파일 생성을 요청합니다"
파일: config.json
내용: { "version": "1.0.0" ... }

[허용] [거부]

→ 사용자가 [허용] 터치
→ Claude Code가 파일 생성 진행
```

---

## 기술적 도전

### 도전 1: 세션 매칭

여러 Claude Code 세션 중 어떤 세션에 명령을 보내야 할까?

```typescript
// 세션 등록 (Claude Code 시작 시)
await supabase.from('sessions').insert({
  session_id: generateUUID(),
  project_path: process.cwd(),
  user_id: userId,
  created_at: new Date()
});

// 세션 조회 (Mobile PWA에서)
const { data: sessions } = await supabase
  .from('sessions')
  .select('*')
  .eq('user_id', userId)
  .order('created_at', { ascending: false });
```

### 도전 2: 실시간성

Polling vs Realtime 선택.

| 방식 | 장점 | 단점 |
|------|------|------|
| Polling | 단순 | 지연, 비효율 |
| WebSocket | 실시간 | 연결 관리 복잡 |
| **Supabase Realtime** | **실시간 + 관리 간편** | Supabase 의존 |

→ Supabase Realtime 선택

### 도전 3: 오프라인 처리

네트워크가 불안정하면?

```typescript
// 요청에 timeout 설정
const request = {
  ...params,
  timeout_at: new Date(Date.now() + 60000) // 60초 후 만료
};

// 만료된 요청 자동 정리
await supabase.rpc('cleanup_expired_requests');
```

---

## Try it yourself: Remote 테스트 해보기

### 1. 테스트 커맨드 실행

```markdown
# Claude Code에서
사용자: /SEMO:permission-test

[SEMO] Remote Test 시작
MCP 도구 semo_remote_request 호출 중...

# Mobile PWA에서 알림 수신
# 승인/거부 선택

✅ 테스트 완료
```

### 2. 대기 중인 요청 확인

```markdown
# Claude Code에서
사용자: /SEMO:remote-pending

[SEMO] 대기 중인 원격 요청:

| ID | 타입 | 메시지 | 생성 시간 |
|----|------|--------|----------|
| req-001 | permission | 파일 생성 허용 | 2분 전 |
| req-002 | selection | 옵션 선택 | 5분 전 |
```

---

## Auto-Command Create: Epic 기반 자동 실행

### 단일 명령의 한계

원격에서 명령을 하나씩 보내는 건 좋았다. 하지만...

"Epic 하나에 Task가 10개인데, 하나씩 수동으로 실행해야 해요?"

GitHub Epic에 연결된 모든 Task를 자동으로 순차 실행하는 기능이 필요했다.

### Auto-Command Creator 탄생

```
┌─────────────────────────────────────────────────────────────┐
│                     Mobile PWA - Settings                    │
│                                                              │
│  [✓] Auto-Command Creator 활성화                             │
│                                                              │
│  Workspace Root: /Users/dev/projects                         │
│  [폴더 선택]                                                  │
│                                                              │
│  → 활성화 시 _controller 세션 자동 생성                        │
│  → workspace 내 프로젝트 자동 프리셋 동기화                     │
└─────────────────────────────────────────────────────────────┘
```

### 작동 방식

#### 1. Controller 세션 생성

Auto-Command 활성화 시, `{workspace_root}/_controller/` 경로에 전용 Controller 세션이 생성된다.

```typescript
// Controller 세션 구조
interface AutoCommandSession {
  id: string;
  controllerSessionId: string;     // Controller 세션
  targetSessionId: string | null;  // 실제 작업할 세션
  currentEpicIssueNumber: number;  // 현재 실행 중인 Epic
  currentTaskIssueNumber: number;  // 현재 실행 중인 Task
  completedTasks: number[];        // 완료된 Task 목록
  status: "idle" | "running" | "paused" | "completed";
}
```

#### 2. Epic 선택 UI

```
┌─────────────────────────────────────────────────────────────┐
│                   Session Detail Modal                       │
│                                                              │
│  🎯 Epic 선택                                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ [v] #142 - 로그인 기능 구현                            │  │
│  │     #135 - 대시보드 리뉴얼                             │  │
│  │     #128 - 결제 시스템 연동                            │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                              │
│  📝 Epic 미리보기:                                           │
│  "사용자 인증 시스템을 구현합니다. JWT 기반..."              │
│                                                              │
│  [🔄 새로고침]  [▶️ 실행 시작]                               │
│                                                              │
│  📊 실행 상태:                                               │
│  - Epic: #142                                                │
│  - 현재 Task: #143 (3/5 완료)                               │
│                                                              │
│  [⏹️ 실행 중지]                                              │
└─────────────────────────────────────────────────────────────┘
```

#### 3. 자동 실행 흐름

```
사용자: Epic #142 선택 → [실행 시작] 클릭
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│                    Controller Session                        │
│                                                              │
│  start_epic_tasks 명령 수신                                   │
│  - epic_issue_number: 142                                    │
│  - target_session_id: "session-abc"                          │
│  - epic_repo: "semicolon-devteam/docs"                       │
│                                                              │
│  1. Epic에서 연결된 Task 목록 조회                            │
│     → #143, #144, #145, #146, #147                           │
│                                                              │
│  2. Task #143 실행 시작                                       │
│     → target session에 프롬프트 전송                          │
│     → 완료 대기                                               │
│                                                              │
│  3. Task #144 실행 시작                                       │
│     → ...                                                    │
│                                                              │
│  4. 모든 Task 완료                                            │
│     → status: "completed"                                    │
└─────────────────────────────────────────────────────────────┘
```

### Epic 캐시 시스템

매번 GitHub API를 호출하면 느리고 비효율적이다.

```typescript
// Epic 목록 캐싱
interface EpicCache {
  user_id: string;
  repo: string;
  epics: {
    number: number;
    title: string;
    body: string;
    labels: string[];
  }[];
  cached_at: string;
}

// 캐시된 Epic 조회 (빠름)
const cachedEpics = await getCachedEpics(userId, repo);

// 새로고침 버튼 클릭 시 갱신
const freshEpics = await fetchEpicList(repo);
await updateEpicCache(userId, repo, freshEpics);
```

### 실전 시나리오: Epic 자동 실행

```text
금요일 오후 3시, 개발자가 모바일로 접속

[Mobile PWA]
1. 세션 목록에서 "cm-land" 프로젝트 선택
2. Epic 드롭다운에서 "#142 로그인 기능" 선택
3. [실행 시작] 버튼 터치

[로컬 Mac - 자동으로]
Controller 세션 활성화
    ↓
Task #143: 로그인 UI 구현 시작
    ↓
(10분 후) Task #143 완료
    ↓
Task #144: 인증 API 구현 시작
    ↓
...

[Mobile PWA - 실시간 업데이트]
📊 진행 상황: 3/5 Task 완료
현재: Task #146 실행 중

[개발자]
회의하면서 진행 상황만 확인
필요하면 [실행 중지] 가능
```

### Workspace 자동 동기화

Auto-Command 활성화 시 workspace 폴더 내 프로젝트들이 자동으로 프리셋에 동기화된다.

```text
Workspace Root: /Users/dev/projects/
    ├── cm-land/        → 프리셋 "cm-land" 자동 생성
    ├── cm-office/      → 프리셋 "cm-office" 자동 생성
    └── core-backend/   → 프리셋 "core-backend" 자동 생성
```

```typescript
// workspace_root 변경 감지 (Realtime)
supabase
  .channel('workspace_root_changes')
  .on('postgres_changes', {
    event: 'UPDATE',
    table: 'user_settings',
    filter: `user_id=eq.${userId}`
  }, async (payload) => {
    if (payload.new.workspace_root !== payload.old.workspace_root) {
      await syncPresetsFromWorkspace(payload.new.workspace_root);
    }
  })
  .subscribe();
```

---

## 의사결정 포인트

### Why: 개발자는 항상 노트북 앞에 있지 않다

- 회의, 이동, 휴식 중에도 작업 확인/승인 필요
- "잠깐만요, 노트북 열게요"의 반복
- 모바일에서 간단한 명령은 가능해야

### Decision: Supabase Realtime + PWA

- DB 기반 상태 관리 (트랜잭션, 영속성)
- Realtime 채널로 즉시 전달
- PWA로 앱 설치 없이 모바일 접근

### Trade-off: 복잡성 증가 vs 사용성

**얻은 것**
- 어디서든 Claude Code 제어
- 비동기 작업 승인
- 팀 협업 가능성

**잃은 것**
- 아키텍처 복잡성
- Supabase 의존성
- 보안 고려사항 증가

---

## 다음 편 예고

원격 제어가 되니, 더 큰 그림이 보이기 시작했다.

"한 명이 여러 Claude를 제어하는 게 아니라, **여러 Agent가 협업하면 어떨까?**"

GatherTown 같은 가상 오피스에서 FE, BE, QA Agent들이 동시에 일하는 세상.

**10편: 가상 오피스 - AI Agent들의 협업 공간**에서 계속됩니다.

---

*이전 편: [SEMO v5.0 - 구조의 대수술](./08-v5-restructuring.md)*
*다음 편: [가상 오피스 - AI Agent들의 협업 공간](./10-virtual-office.md)*
