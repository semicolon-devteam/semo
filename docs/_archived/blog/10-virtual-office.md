# 10편: 가상 오피스 - AI Agent들의 협업 공간

> 시리즈: AI와 함께 일하는 법을 만들다 (10/10, 완결)

---

## 프롤로그: "GatherTown에서 AI들이 일하면 어떨까?"

2025년 1월, SEMO Remote v0.4.0 안정화 후.

원격 제어가 가능해지자, 더 큰 질문이 떠올랐다.

"한 명이 하나의 Claude를 조종하는 거 말고... **여러 AI가 동시에 협업하면** 어떨까?"

팀의 개발 문화를 떠올려봤다. GatherTown에서 디자이너, 프론트엔드, 백엔드, QA가 각자의 자리에서 일하다가, 필요할 때 모여서 논의한다.

> **"AI Agent들도 가상 오피스에서 협업할 수 있지 않을까?"**

---

## SEMO Office의 탄생

### 비전: 멀티에이전트 가상 오피스

```text
                        GatherTown 가상 오피스
        ┌───────────────────────────────────────────────┐
        │                                               │
        │   🧑‍💼 PO        🎨 Designer                   │
        │    (John)        (Sally)                     │
        │                                               │
        │                                               │
        │   💻 FE Dev     💾 BE Dev      🧪 QA         │
        │    (Dave)        (Alex)       (Murat)        │
        │                                               │
        │                    📋                         │
        │              [작업 보드]                       │
        └───────────────────────────────────────────────┘

        사용자: "로그인 기능 구현해줘"
                        │
                        ▼
        FE, BE, QA 각자의 Worktree에서 병렬 작업
                        │
                        ▼
        의존성 순서에 따라 PR 자동 머지
```

### 핵심 개념

| 개념 | 설명 |
|------|------|
| **Office** | GitHub 레포지토리와 매핑된 가상 작업 공간 |
| **Agent** | 특정 역할(FE, BE, QA)을 가진 AI 작업자 |
| **Persona** | Agent의 성격, 전문 영역, 업무 스타일 |
| **Worktree** | Agent별 독립된 Git 작업 디렉토리 |
| **Job** | Agent가 수행할 단위 작업 |

---

## 아키텍처: Task Decomposition

### 자연어 → 역할별 Job 분해

가장 핵심적인 기능: **Task Decomposer**.

```text
사용자: "로그인 기능 구현해줘"
              │
              ▼
       ┌──────────────────┐
       │ Task Decomposer  │
       │                  │
       │ NLU 분석:        │
       │ - 기능: 인증     │
       │ - 유형: create   │
       │ - 복잡도: 중간   │
       └────────┬─────────┘
                │
     ┌──────────┼──────────┐
     │          │          │
     ▼          ▼          ▼
  Job #1     Job #2     Job #3
  (FE Dev)   (BE Dev)   (QA)
  로그인 UI  로그인 API  테스트

  priority:1 priority:1 priority:2
  deps: []   deps: []   deps: [#1,#2]
```

### Job 구조

```typescript
interface Job {
  id: string;
  description: string;        // "로그인 UI 구현 및 프론트엔드 개발"
  role: "FE" | "BE" | "QA" | "DevOps" | "PO";
  status: "pending" | "ready" | "running" | "completed" | "failed";
  priority: number;           // 1이 가장 높음
  depends_on: string[];       // 의존 Job ID 목록
  worktree_path: string;      // "/repo/.worktrees/agent-fe"
  pr_number?: number;         // 생성된 PR 번호
}
```

### 실행 순서 결정

```typescript
// Task Decomposer 응답 예시
{
  "message": "Task decomposed and enqueued",
  "summary": "로그인 - create",
  "jobs": [
    {
      "id": "job-fe-123",
      "description": "로그인 UI 구현 및 프론트엔드 개발",
      "status": "ready",
      "priority": 1
    },
    {
      "id": "job-be-124",
      "description": "로그인 API 구현 및 백엔드 개발",
      "status": "ready",
      "priority": 1
    },
    {
      "id": "job-qa-125",
      "description": "로그인 테스트 작성 및 품질 검증",
      "status": "pending",
      "depends_on": ["job-fe-123", "job-be-124"],
      "priority": 2
    }
  ],
  "execution_order": [
    ["job-fe-123", "job-be-124"],  // 1단계: FE, BE 병렬
    ["job-qa-125"]                  // 2단계: QA (의존성 완료 후)
  ]
}
```

---

## Git Worktree: 충돌 없는 병렬 작업

### 문제: 같은 레포에서 여러 Agent가 동시에 작업하면?

```text
Before (단일 작업 디렉토리):
/cm-land/
    ├── src/
    │   └── Login.tsx  ← FE Agent가 수정 중
    │                  ← BE Agent도 접근? 💥 충돌!
    └── ...
```

### 해결: Git Worktree로 물리적 격리

```text
After (Worktree 격리):
/cm-land/                    # main 브랜치 (건드리지 않음)
    ├── src/
    └── .worktrees/
        ├── agent-fe/        # feature/login-ui 브랜치
        │   └── src/Login.tsx  ← FE Agent 전용
        ├── agent-be/        # feature/login-api 브랜치
        │   └── src/api/     ← BE Agent 전용
        └── agent-qa/        # feature/login-test 브랜치
            └── tests/       ← QA Agent 전용
```

### Worktree 스킬

```markdown
## create-worktree

Agent용 독립 작업 공간 생성

[SEMO] Skill: create-worktree 호출

입력:
- agent_role: "fe"
- branch_name: "feature/login-ui"
- base_branch: "main"

출력:
- worktree_path: "/cm-land/.worktrees/agent-fe"
- branch: "feature/login-ui"

✅ Worktree 생성 완료
```

```bash
# 내부적으로 실행되는 Git 명령
git worktree add .worktrees/agent-fe -b feature/login-ui main
```

---

## PR 기반 워크플로우

### 의존성 순서에 따른 자동 머지

```text
1단계: FE, BE 병렬 작업 완료
    │
    ▼
PR #101 (FE): feature/login-ui → main
PR #102 (BE): feature/login-api → main
    │
    ▼
2단계: QA 작업 완료
    │
    ▼
PR #103 (QA): feature/login-test → main
              depends_on: [#101, #102]
    │
    ▼
merge-pr 스킬 실행:
    1. #101 머지 (FE)
    2. #102 머지 (BE)
    3. #103 머지 (QA) ← 의존성 완료 후
```

### merge-pr 스킬

```typescript
async function mergePRsInOrder(prs: PR[]): Promise<void> {
  // 토폴로지 정렬로 머지 순서 결정
  const sortedPRs = topologicalSort(prs);

  for (const pr of sortedPRs) {
    // 의존 PR들이 모두 머지됐는지 확인
    const depsComplete = pr.depends_on.every(
      depId => prs.find(p => p.id === depId)?.status === 'merged'
    );

    if (depsComplete) {
      await mergePR(pr.number);
      pr.status = 'merged';
    }
  }
}
```

---

## Office Server + Remote Client 통합

### 전체 흐름

```text
┌─────────────────────────────────────────────────────────────┐
│                     Office Server                            │
│                  (Express + Supabase)                        │
│                                                              │
│  Task Decomposer → Job Scheduler → Session Executor          │
│                                              │               │
└──────────────────────────────────────────────│───────────────┘
                                               │
                                               ▼
                      ┌────────────────────────────────────────┐
                      │         Supabase (PostgreSQL)          │
                      │                                        │
                      │  agent_commands (Realtime 활성화)      │
                      │  agent_command_results                 │
                      │  agent_sessions                        │
                      └────────────────────────────────────────┘
                                               │
                                               │ Realtime Subscribe
                                               ▼
┌─────────────────────────────────────────────────────────────┐
│                 semo-remote-client (Electron)                │
│                                                              │
│   Office Command Subscriber                                  │
│   - agent_commands 테이블 구독                                │
│   - office_id 필터링                                         │
│   - command_type별 핸들러 실행                                │
│                              │                               │
│              ┌───────────────┼───────────────┐              │
│              ▼               ▼               ▼              │
│       ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│       │ iTerm Tab│    │ iTerm Tab│    │ iTerm Tab│         │
│       │ Agent:FE │    │ Agent:BE │    │ Agent:QA │         │
│       │ Worktree │    │ Worktree │    │ Worktree │         │
│       └──────────┘    └──────────┘    └──────────┘         │
└─────────────────────────────────────────────────────────────┘
```

### Agent Command Types

| Type | 설명 | Payload |
|------|------|---------|
| `create_session` | 새 Claude Code 세션 | `{ worktree_path, persona_name, initial_prompt }` |
| `send_prompt` | 프롬프트 전송 | `{ prompt }` |
| `send_text` | 텍스트 입력 | `{ text }` |
| `get_output` | 출력 조회 | `{ lines: 100 }` |
| `cancel` | 작업 취소 (Ctrl+C) | `{}` |
| `terminate` | 세션 종료 | `{}` |

---

## Agent Personas

### BMad Method 영감

각 Agent는 고유한 페르소나를 가진다. BMad Method에서 영감을 받아 14개의 역할별 페르소나를 정의했다.

| Agent | 페르소나 | 전문 영역 | 코어 스킬 |
|-------|---------|----------|----------|
| **PO** | John | 기획서, 명세서 | generate-spec |
| **PM** | Bob | 일정 관리 | planner, notify-slack |
| **Architect** | Winston | 시스템 설계 | planner, write-code |
| **FE Dev** | Dave | UI/컴포넌트 | write-code, create-pr |
| **BE Dev** | Alex | API/서버 | write-code, create-pr |
| **QA** | Murat | 테스트 | write-test |
| **DevOps** | Dan | 배포/인프라 | deployer |

### 페르소나 적용

```typescript
// Agent 세션 생성 시 페르소나 주입
const prompt = `
당신은 ${persona.name}입니다.
역할: ${persona.role}
전문 영역: ${persona.expertise.join(', ')}
업무 스타일: ${persona.workStyle}

현재 작업:
${job.description}

Worktree 경로: ${job.worktree_path}
브랜치: ${job.branch_name}
`;

await sendCommand({
  type: 'create_session',
  payload: {
    worktree_path: job.worktree_path,
    persona_name: persona.name,
    initial_prompt: prompt
  }
});
```

---

## 2D Office UI

### PixiJS 기반 실시간 시각화

```text
┌─────────────────────────────────────────────────────────────┐
│                    SEMO Office - cm-land                     │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                                                     │    │
│  │   🧑‍💼                                              │    │
│  │   PO (idle)                                        │    │
│  │                                                     │    │
│  │         💻 ●                     💾 ●              │    │
│  │         FE (working)             BE (working)      │    │
│  │         PR #101 생성 중           API 구현 중        │    │
│  │                                                     │    │
│  │                     🧪                              │    │
│  │                     QA (waiting)                   │    │
│  │                     FE, BE 대기 중                  │    │
│  │                                                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  📊 진행 상황: 2/3 Agent 작업 중 | Job: 2/3 완료            │
│                                                              │
│  [새 작업 요청]  [Agent 추가]  [로그 보기]                    │
└─────────────────────────────────────────────────────────────┘
```

### 실시간 상태 동기화

```typescript
// Supabase Realtime으로 Agent 상태 구독
supabase
  .channel('office_agents')
  .on('postgres_changes', {
    event: '*',
    schema: 'public',
    table: 'office_agents',
    filter: `office_id=eq.${officeId}`
  }, (payload) => {
    // PixiJS 캔버스 업데이트
    updateAgentSprite(payload.new);
  })
  .subscribe();
```

---

## 실전 시나리오

### 시나리오: "로그인 기능 구현해줘"

```text
09:00 - 사용자가 Office UI에서 작업 요청

[Office Server]
POST /api/offices/{office_id}/tasks
{ "task": "로그인 기능 구현해줘" }

09:01 - Task Decomposer 분석

[응답]
{
  "jobs": [
    { "role": "FE", "desc": "로그인 UI 구현" },
    { "role": "BE", "desc": "로그인 API 구현" },
    { "role": "QA", "desc": "로그인 테스트 작성", "deps": ["FE", "BE"] }
  ]
}

09:02 - Worktree 생성 & Agent 세션 시작

[semo-remote-client]
iTerm 탭 3개 생성:
- Agent:FE (.worktrees/agent-fe)
- Agent:BE (.worktrees/agent-be)
- Agent:QA (.worktrees/agent-qa) - 대기 중

09:03~09:30 - FE, BE 병렬 작업

[Agent:FE]
✅ Login.tsx 구현
✅ useAuth 훅 구현
✅ PR #101 생성

[Agent:BE]
✅ /api/auth/login 구현
✅ JWT 미들웨어 구현
✅ PR #102 생성

09:31 - QA 작업 시작 (의존성 완료)

[Agent:QA]
✅ login.spec.ts 작성
✅ PR #103 생성

09:35 - 자동 머지

[merge-pr 스킬]
1. PR #101 머지 (FE)
2. PR #102 머지 (BE)
3. PR #103 머지 (QA)

✅ 로그인 기능 구현 완료!
```

---

## Tech Stack

| Layer | Technology |
|-------|------------|
| Backend | Express.js, TypeScript |
| Frontend | Next.js 14, PixiJS, Tailwind CSS |
| State | Zustand |
| Database | Supabase (PostgreSQL) |
| Realtime | Supabase Realtime |
| Git | simple-git |

---

## Roadmap

### Phase 1 (MVP) ✅

- [x] DB 스키마 설계 및 마이그레이션
- [x] Task Decomposer 기본 구현
- [x] Worktree Manager
- [x] 기본 API 엔드포인트
- [x] 2D Office UI (PixiJS)

### Phase 2 (진행 중)

- [ ] Claude Code 세션 연동
- [ ] 실시간 Agent 상태 동기화
- [ ] PR 자동 생성/병합

### Phase 3 (계획)

- [ ] 멀티 레포지토리 지원
- [ ] 커스텀 페르소나 UI 에디터
- [ ] 대시보드 및 리포트

---

## 의사결정 포인트

### Why: 복잡한 기능은 여러 전문가가 협업해야

- 로그인 하나에도 FE, BE, QA 작업 필요
- 순차 실행은 너무 느림
- 병렬 실행하려면 충돌 방지 필요

### Decision: Git Worktree + Task Decomposition

- Worktree로 물리적 작업 공간 격리
- NLU로 자연어 → 역할별 Job 분해
- 의존성 그래프로 실행 순서 결정
- PR 기반으로 안전한 코드 병합

### Trade-off: 복잡성 vs 생산성

**얻은 것**
- 3명의 작업을 1/3 시간에
- 충돌 없는 병렬 작업
- 자동화된 PR 워크플로우

**잃은 것**
- 시스템 복잡성 증가
- 인프라 비용 (Supabase, 멀티 세션)
- 초기 설정 비용

---

## 시리즈를 마치며

### 11개월의 여정

```text
2024-02  SAX 탄생 (11개 역할 기반 패키지)
    ↓
2024-06  토큰 위기 (55% 최적화)
    ↓
2024-12  SAX → SEMO 리브랜딩
    ↓
2024-12  기능 기반 3-Layer 전환
    ↓
2024-12  v5.0 대수술 (7개 패키지)
    ↓
2025-01  SEMO Remote (원격 제어)
    ↓
2025-01  SEMO Office (멀티에이전트 협업)
```

### 배운 것들

1. **AI는 팀원처럼 대해야 한다**
   - 명확한 역할과 책임
   - 컨텍스트 공유
   - 피드백 루프

2. **토큰 경제학은 핵심이다**
   - Progressive Disclosure
   - 필요할 때만 로드
   - 중복 제거

3. **자동화가 답이다**
   - 30초 설치
   - 원격 제어
   - 멀티에이전트 협업

4. **복잡성을 두려워하지 마라**
   - v5.0 Breaking Change
   - Git Worktree 도입
   - Task Decomposition

### 앞으로의 방향

SEMO Office는 시작일 뿐이다.

- **실시간 협업**: Agent 간 실시간 소통
- **학습하는 AI**: 팀 컨벤션 학습
- **자율 운영**: 사람 개입 최소화

AI 에이전트가 진짜 팀원처럼 일하는 세상.

SEMO가 그 길을 열어가겠습니다.

---

## 감사의 말

10편의 긴 여정을 함께해주셔서 감사합니다.

SEMO는 오픈소스입니다. 기여, 피드백 모두 환영합니다:

- [GitHub](https://github.com/semicolon-devteam/semo)
- [피드백](/SEMO:feedback)

> **"AI와 함께 일하는 법을 만들다"**
>
> 이제 당신의 팀도 시작해보세요.

```bash
npx @team-semicolon/semo-cli init
```

---

*이전 편: [원격 제어 - 모바일에서 Claude Code를 다루다](./09-remote-control.md)*
*시리즈 목차: [AI와 함께 일하는 법을 만들다](./00-series-index.md)*
