---
name: generate-meeting-template
description: |
  정기회의록 템플릿 자동 생성. 기간 정보를 입력받아 회의 템플릿 생성.
  Use when (1) "정기회의록 만들어줘", (2) "회의 템플릿 생성",
  (3) "25년 12월 4/4 회의록" 형식 요청.
tools: [Bash, Read, Glob, AskUserQuestion]
model: inherit
---

> **시스템 메시지**: 이 Skill이 호출되면 `[SEMO] Skill: generate-meeting-template 호출` 메시지를 첫 줄에 출력하세요.

# generate-meeting-template Skill

> 정기회의 템플릿 자동 생성

## Purpose

정기회의(주간/격주 회의)의 템플릿을 자동 생성합니다:
1. 기간 정보 파싱 (년도, 월, 주차)
2. 해당 기간 STT 파일 검색 (선택)
3. 회의 템플릿 마크다운 생성
4. GitHub Discussion 생성 또는 초안 제공

## Trigger Keywords

- "정기회의록 만들어줘", "회의록 템플릿"
- "회의 템플릿 생성", "미팅 템플릿"
- "25년 12월 4/4 회의록" (기간 형식)
- "이번주 회의록", "이번 주 정기회의"

## Input

### 필수 파라미터

| 파라미터 | 타입 | 설명 |
|----------|------|------|
| `period` | string | 기간 정보 (예: "25년 12월 4/4", "2025년 1월 1주차") |

### 선택 파라미터

| 파라미터 | 타입 | 설명 |
|----------|------|------|
| `meeting_type` | enum | 회의 유형 (기본: `정기 회고`) |
| `search_stt` | boolean | STT 파일 검색 여부 (기본: true) |
| `create_discussion` | boolean | Discussion 자동 생성 (기본: false, 확인 후 생성) |
| `participants` | string[] | 참석자 목록 (없으면 기본 팀원) |

### meeting_type Enum

- `정기 회고` (기본)
- `스프린트 회고`
- `기술 회의`
- `기획 회의`

## Workflow

```text
기간 정보 입력 (예: "25년 12월 4/4")
    │
    ├─ 1. 기간 파싱
    │   └→ year: 2025, month: 12, week: 4
    │
    ├─ 2. STT 파일 검색 (선택)
    │   └→ docs/meetings/2025-12-* 패턴 검색
    │
    ├─ 3. 템플릿 생성
    │   └→ 마크다운 템플릿 작성
    │
    ├─ 4. 사용자 확인
    │   └→ "이 템플릿으로 Discussion 생성할까요?"
    │
    └─ 5. Discussion 생성 (승인 시)
        └→ command-center 레포에 생성
```

## Execution

### Step 1: 기간 파싱

```bash
# 입력 예시: "25년 12월 4/4"
PERIOD="{period}"

# 년도 추출 (2자리 → 4자리)
YEAR=$(echo "$PERIOD" | grep -oE '[0-9]{2,4}년' | sed 's/년//')
if [ ${#YEAR} -eq 2 ]; then
  YEAR="20$YEAR"
fi

# 월 추출
MONTH=$(echo "$PERIOD" | grep -oE '[0-9]{1,2}월' | sed 's/월//')

# 주차 추출 (n/n 또는 n주차 형식)
WEEK=$(echo "$PERIOD" | grep -oE '[0-9]/[0-9]|[0-9]주차' | head -1)
if [[ "$WEEK" =~ / ]]; then
  WEEK=$(echo "$WEEK" | cut -d'/' -f1)
else
  WEEK=$(echo "$WEEK" | sed 's/주차//')
fi

echo "Parsed: $YEAR년 $MONTH월 $WEEK주차"
```

### Step 2: STT 파일 검색

```bash
# docs/meetings/ 디렉토리에서 해당 기간 파일 검색
STT_PATTERN="docs/meetings/${YEAR}-${MONTH}*.md"

# 또는 meetings/ 하위 검색
find docs/meetings -name "*${YEAR}*${MONTH}*" -type f 2>/dev/null
```

### Step 3: 템플릿 생성

회의 템플릿 형식:

```markdown
# [{month}월 {week}주차] 정기 회고 & 회의

> 📅 일시: {date}
> 👥 참석자: {participants}
> 📍 장소: 화상 회의 (Google Meet)

---

## 📋 안건

### 1. 지난주 리뷰
- [ ] 완료된 작업
- [ ] 이월된 작업

### 2. 이번주 계획
- [ ] 주요 목표
- [ ] 담당자 배정

### 3. 공유 사항
- [ ] 공지사항
- [ ] 이슈/블로커

### 4. 기타 논의
- [ ] 자유 토론

---

## ✅ 결정사항

| # | 결정 내용 | 담당 | 기한 |
|---|----------|------|------|
| 1 | - | - | - |

---

## 📌 액션 아이템

| # | 할 일 | 담당자 | 기한 | 상태 |
|---|-------|--------|------|------|
| 1 | - | - | - | ⬜ |

---

## 📎 참고 자료

- STT 스크립트: {stt_file_link}
- 이전 회의록: {previous_meeting_link}
```

### Step 4: Discussion 생성

```bash
# 본문 파일 저장
cat > /tmp/meeting-template.md << 'EOF'
{generated_template}
EOF

# Discussion 생성
gh discussion create \
  --repo semicolon-devteam/command-center \
  --category "회의록" \
  --title "[{month}월 {week}주차] 정기 회고 & 회의" \
  --body-file /tmp/meeting-template.md
```

## Output

### 성공 (Discussion 생성)

```markdown
[SEMO] Skill: generate-meeting-template 완료

✅ 정기회의록 템플릿 생성 완료

**기간**: 2025년 12월 4주차
**회의 유형**: 정기 회고
**Discussion**: https://github.com/semicolon-devteam/command-center/discussions/XXX

**발견된 STT 파일**:
- docs/meetings/2025-12-23-weekly.md
- docs/meetings/2025-12-27-standup.md

다음 단계: 회의 후 `summarize-meeting` 스킬로 내용 정리
```

### 성공 (초안만 제공)

```markdown
[SEMO] Skill: generate-meeting-template 완료

✅ 정기회의록 템플릿 생성 (초안)

**기간**: 2025년 12월 4주차

---
{template_content}
---

Discussion으로 생성하려면 "Discussion 만들어줘"라고 말씀해주세요.
```

### 실패

```markdown
[SEMO] Skill: generate-meeting-template 실패

❌ 기간 파싱 실패

**입력**: {period}
**원인**: 인식할 수 없는 기간 형식

**지원 형식**:
- "25년 12월 4/4"
- "2025년 1월 1주차"
- "이번주", "지난주"
```

## Quick Start

### 기본 사용

```yaml
period: "25년 12월 4/4"
```

### 상세 옵션

```yaml
period: "25년 12월 4/4"
meeting_type: "정기 회고"
participants: ["@reus-jeon", "@garden92", "@kyago"]
create_discussion: true
```

### 간단 호출

```
"25년 12월 4/4 정기회의록 만들어줘"
"이번주 회의록 템플릿 생성해줘"
```

## Related

- [summarize-meeting](../summarize-meeting/SKILL.md) - STT 분석 후 회의록 작성 (이 스킬 이후 호출)
- [create-decision-log](../create-decision-log/SKILL.md) - 의사결정 로그 생성
