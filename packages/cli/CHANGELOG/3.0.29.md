# 3.0.29 (2025-12-26)

## Changed

- **덮어쓰기 기본값 변경**: `semo init` 및 `semo add` 명령어에서 기존 파일 덮어쓰기 확인 시 기본값을 `Y`로 변경
  - Before: `... 덮어쓰시겠습니까? (y/N)` → 기본 N
  - After: `... 덮어쓰시겠습니까? (Y/n)` → 기본 Y
  - SEMO 업데이트 시 엔터만 누르면 바로 덮어쓰기 진행
