# SEMO CLI v3.11.0 Changelog

> Release Date: 2026-01-14

## 🚀 Major Changes

### Supabase DB 기반 패키지 관리

CLI가 하드코딩된 패키지 정보 대신 Supabase `package_definitions` 테이블에서 패키지 정보를 조회합니다.

#### 주요 변경사항

1. **새 의존성 추가**
   - `@supabase/supabase-js` v2.49.1

2. **새 파일 추가**
   - `src/supabase.ts` - Supabase 클라이언트 및 패키지 조회 함수

3. **DB 연결 폴백**
   - DB 연결 실패 시 기존 하드코딩된 데이터로 자동 폴백
   - 오프라인 환경에서도 정상 동작 보장

## 📦 영향받는 명령어

| 명령어 | 변경 내용 |
|--------|----------|
| `semo init` | 패키지 데이터 DB에서 조회 |
| `semo add` | 패키지 데이터 DB에서 조회 |
| `semo list` | 패키지 목록 DB에서 조회 |
| `semo update` | 설치된 패키지 DB 기반 확인 |

## 📋 데이터베이스 스키마

### package_definitions 테이블

```sql
CREATE TABLE package_definitions (
  id UUID PRIMARY KEY,
  name VARCHAR(100) NOT NULL UNIQUE,
  display_name VARCHAR(100) NOT NULL,
  description TEXT,
  layer VARCHAR(20) NOT NULL,  -- standard, biz, eng, ops, system, meta
  package_type VARCHAR(20),    -- standard, extension
  version VARCHAR(20),
  source_path VARCHAR(255),
  detect_files TEXT[],
  aliases TEXT[],
  install_order INTEGER,
  is_active BOOLEAN,
  is_required BOOLEAN
);
```

## 🔄 호환성

- 기존 `EXTENSION_PACKAGES`, `SHORTNAME_MAPPING` 상수는 폴백용으로 유지
- DB 연결 불가 시 기존 방식으로 동작
- 기존 설치된 SEMO 환경과 완전 호환

## 마이그레이션 (Supabase)

```bash
# 마이그레이션 파일 적용
supabase migration up

# 시드 데이터 적용
supabase db seed
```

## 관련 파일

- `supabase/migrations/20260114000_package_definitions.sql`
- `supabase/migrations/20260114001_package_definitions_seed.sql`
