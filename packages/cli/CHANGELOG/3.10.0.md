# 3.10.0

## Changed

- **`semo init` 인터랙션 제거**: 프로젝트 감지 후 패키지 설치 프롬프트 제거
  - Next.js, Spring 등 프로젝트 감지 시 자동 설치 제안 프롬프트 제거
  - 패키지 선택 checkbox 프롬프트 제거
  - Standard (semo-core + semo-skills)만 기본 설치

- **프로젝트 감지는 정보 제공용으로만 사용**
  - 감지된 프로젝트 유형을 안내만 하고, 설치는 `semo add`로 유도
  - `--with` 옵션으로 명시적 패키지 지정 시에만 Extension 함께 설치

## Fixed

- Windows 환경에서 `semo init` 실행 시 불필요한 인터랙션 발생 문제 수정

## 새로운 동작

**Before (v3.9.0)**:
```
$ semo init
📦 감지된 프로젝트 유형:
   - Next.js: Next.js 프론트엔드 개발
? 감지된 패키지를 함께 설치할까요? (Y/n)  ← 인터랙션 발생
```

**After (v3.10.0)**:
```
$ semo init
💡 감지된 프로젝트 유형:
   - Next.js: Next.js 프론트엔드 개발
   추가 패키지가 필요하면: semo add nextjs
                           ← 인터랙션 없이 바로 설치 진행
```

## 사용법

```bash
# 기본 설치 (Standard만)
semo init

# Extension과 함께 설치 (명시적 지정)
semo init --with next,hooks
```
