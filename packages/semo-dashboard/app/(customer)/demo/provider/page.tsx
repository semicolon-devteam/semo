import { ScreenProvider } from '../../_ui/screen-provider';

/** 공개 데모 — Provider · Agent Factory & Curation (mock). */
export default function DemoProviderPage() {
  return <ScreenProvider tab="review" />;
}
