# Audit Checks

> audit-issues Skill의 4가지 감사 항목 상세 로직

## 1. 중복 이슈 검토

### 탐지 알고리즘

```
1. 모든 Open 이슈 목록 조회
2. 이슈 쌍(pair)별로 유사도 계산
3. 유사도 임계값 초과 시 중복 후보로 표시
```

### 유사도 계산

**제목 유사도** (가중치: 60%)
- Levenshtein Distance 기반
- 한글/영문 토큰화 후 비교
- 임계값: 80%

**본문 유사도** (가중치: 40%)
- 키워드 추출 (불용어 제거)
- Jaccard Similarity
- 임계값: 70%

**최종 유사도**:
```
final_score = (title_sim * 0.6) + (body_sim * 0.4)
duplicate_threshold = 0.75
```

### 예외 처리

- 에픽 이슈는 중복 검사에서 제외 (의도적 분할 가능)
- 서로 다른 레포지토리 간 이슈는 별도 그룹으로 표시
- 부모-자식 관계 이슈는 중복으로 판단하지 않음

---

## 2. 필수 필드 누락 검토

### 필수 필드 정의

| 필드 | 필드 ID | 필수 조건 |
|------|---------|----------|
| **타입** | `타입` | 모든 이슈 |
| **우선순위** | `우선순위` | 모든 이슈 |
| **Status** | `Status` | 모든 이슈 (기본값 있음) |
| **기술영역** | `기술영역` | 권장 (경고만) |

### 검사 로직

```python
def check_required_fields(item):
    missing = []

    # 타입 검사
    if not get_field_value(item, "타입"):
        missing.append("타입")

    # 우선순위 검사
    if not get_field_value(item, "우선순위"):
        missing.append("우선순위")

    return missing
```

### 필드 값 검증

- `null` → 누락
- 빈 문자열 → 누락
- `None` 선택값 → 누락

---

## 3. Projects 미연결 검토

### 검사 대상 레포지토리

```yaml
repositories:
  - semicolon-app
  - semicolon-web
  - semicolon-api
  - core-supabase
  - docs
  - sax
```

### 검사 로직

```bash
# 1. 각 레포지토리의 Open 이슈 조회
gh issue list --repo semicolon-devteam/{repo} --state open --json number,title,projectItems

# 2. projectItems 배열 확인
# 3. "이슈관리" 프로젝트 연결 여부 체크
```

### 연결 상태 판단

```python
def is_connected_to_project(issue, project_name="이슈관리"):
    for project in issue.get("projectItems", []):
        if project.get("title") == project_name:
            return True
    return False
```

### 예외 처리

- `wontfix`, `duplicate` 라벨이 있는 이슈는 제외
- 7일 이내 생성된 이슈는 경고만 (연결 대기 가능)

---

## 4. task 이슈 작업량 미할당 검토

### 검사 조건

```
타입 == "태스크" AND 작업량 == null
```

### 작업량 필드

- **필드 ID**: `PVTF_lADOC01-Rc4AtDz2zg0bhf0`
- **타입**: Number
- **단위**: 스토리 포인트 (1, 2, 3, 5, 8, 13)

### 검사 로직

```python
def check_task_workload(item):
    # 타입 확인
    type_value = get_field_value(item, "타입")
    if type_value != "태스크":
        return None  # 검사 대상 아님

    # 작업량 확인
    workload = get_field_value(item, "작업량")
    if workload is None or workload == 0:
        return "작업량 미할당"

    return None  # 정상
```

### 예외 처리

- Status가 "버려짐"인 이슈는 제외
- "spike", "research" 라벨이 있는 이슈는 경고만 (추정 어려움)

---

## 감사 실행 순서

```
1. 이슈관리 프로젝트 전체 아이템 조회
2. 각 레포지토리 Open 이슈 조회 (Projects 미연결 검사용)
3. 중복 이슈 검토 → 결과 저장
4. 필수 필드 누락 검토 → 결과 저장
5. Projects 미연결 검토 → 결과 저장
6. 작업량 미할당 검토 → 결과 저장
7. 통합 리포트 생성
```

## 심각도 분류

| 심각도 | 항목 | 조치 |
|--------|------|------|
| 🔴 Critical | 필수 필드 누락 | 즉시 수정 필요 |
| 🟠 High | 작업량 미할당 | 스프린트 전 수정 |
| 🟡 Medium | Projects 미연결 | 주간 정리 시 처리 |
| 🟢 Low | 중복 의심 | 검토 후 판단 |
