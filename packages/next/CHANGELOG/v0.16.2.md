# v0.16.2

> Release Date: 2024-11-28

## Summary

Skill 규칙 준수 수정: `frontend-design`, `fast-track` SKILL.md에 YAML frontmatter 추가

## Fixed

### YAML Frontmatter 추가

기존 Metadata 섹션(코드블록)을 표준 YAML frontmatter로 변환:

| Skill | 변경 내용 |
|-------|----------|
| `frontend-design` | `---` frontmatter 블록 추가 (name, description, tools, triggers) |
| `fast-track` | `---` frontmatter 블록 추가 (name, description, tools, triggers) |

### 추가된 필수 요소

- **description**: "Use when (1)..., (2)..." 형식 준수
- **tools**: 도구 배열 명시
- **시스템 메시지 템플릿**: `> **🔔 시스템 메시지**:` 추가

## Notes

- 기존 Metadata 섹션(```yaml 코드블록)은 제거
- 기능 변경 없음 (PATCH 버전)
- Skills 규칙 검증 결과 반영
