# v0.44.1

> 2024-12-05

## Fixed

- `install-sax.sh` 파이프 실행 시 입력 대기 문제 수정
  - `curl | bash` 실행 시 stdin이 스크립트가 되어 `read` 명령 실패
  - `read -r choice </dev/tty`로 터미널에서 직접 입력 받도록 수정

## Context

- 파이프라인 실행: `curl -fsSL ... | bash`
- 직접 실행: `./install-sax.sh` (기존과 동일하게 동작)
