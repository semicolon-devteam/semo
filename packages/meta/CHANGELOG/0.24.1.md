# 0.24.1

> install-sax.sh commands 경로 버그 수정

## Fixed

- **install-sax.sh**: commands 설치 경로 수정
  - Before: `.claude/SAX/commands/` (잘못된 경로)
  - After: `.claude/commands/SAX/` (올바른 경로)
  - 커맨드가 `/SAX:help` 형태로 올바르게 동작하도록 수정

## Notes

기존 잘못 설치된 `.claude/SAX/` 디렉토리는 수동 삭제 필요:
```bash
rm -rf .claude/SAX
```
