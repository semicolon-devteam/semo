# v0.28.0

> 2025-12-10

## Added

### _shared 심링크 지원
- `install-sax.sh`에 `_shared` 심링크 생성 로직 추가
  - `.claude/_shared → sax-core/_shared` 심링크 자동 생성
  - Windows 환경에서는 복사 모드로 동작
- `sax-architecture-checker`에 `_shared` 심링크 검증 추가
  - 심링크 존재 및 대상 경로 유효성 검증
  - 자동 수정 로직 포함

## Changed

### CLAUDE.md 참조 경로 표준화 (8개 패키지)

모든 SAX 패키지의 CLAUDE.md에서 `_shared/` 참조 경로를 심링크 기반으로 변경:

| 변경 전 | 변경 후 |
|---------|---------|
| `sax-core/_shared/INIT_SETUP.md` | `../_shared/INIT_SETUP.md` |
| `sax-core/_shared/SAX_CORE_REFERENCE.md` | `../_shared/SAX_CORE_REFERENCE.md` |
| `sax-core/_shared/ORCHESTRATOR_RULES.md` | `../_shared/ORCHESTRATOR_RULES.md` |

**대상 패키지**: sax-next, sax-po, sax-qa, sax-backend, sax-ms, sax-pm, sax-design, sax-infra

### deploy-workflow.md 업데이트
- 배포 구조에 `_shared` 심링크 포함
- 심링크 기반 참조 경로 설명 추가
