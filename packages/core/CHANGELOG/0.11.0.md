# 0.11.0 (2025-12-02)

## Added

- **`claude-health` 스킬 신규 추가**
  - `.claude` 디렉토리 구조 검증 및 자동 수정
  - 심링크 무결성 체크 (CLAUDE.md, agents/, skills/, commands/SAX/)
  - install-sax.sh와 동일한 로직으로 자동 복구
  - `version-updater` 업데이트 완료 후 자동 호출

- **`/SEMO:health` 커맨드 추가**
  - claude-health 스킬 수동 호출

## Changed

- **`version-updater` 스킬 개선**
  - 업데이트 완료 후 claude-health 자동 호출 단계 추가
  - install-sax.sh 없이도 심링크 자동 관리

## Notes

- 이제 SEMO 업데이트 시 `install-sax.sh --update`를 별도로 실행할 필요 없음
- version-updater가 서브모듈 업데이트 후 자동으로 심링크 검증 및 수정
