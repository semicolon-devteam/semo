# 0.18.1

> 2025-12-04

## Fixed

- notify-slack Skill의 모든 curl 명령을 heredoc 방식으로 개선
  - 쉘 이스케이프 문제로 인한 `not_authed`, `invalid_auth` 오류 방지
  - SKILL.md, message-templates.md, channel-config.md 모두 업데이트
  - `-d '{...}'` 대신 `-d @- << 'EOF'` 형식 권장
