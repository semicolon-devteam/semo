---
name: report-bug
description: |
  QA 테스트 중 발견된 버그를 서비스 레포에 이슈카드로 생성. Use when (1) QA 테스트 중 버그 발견,
  (2) 테스트 케이스 실패 시 버그 리포팅, (3) 브랜치 생성까지 원스톱 처리 필요.
tools: [Bash, Read]
model: inherit
---

> **시스템 메시지**: 이 Skill이 호출되면 `[SEMO] Skill: report-bug 호출` 시스템 메시지를 첫 줄에 출력하세요.

# report-bug Skill (QA)

> QA 테스트 중 발견된 버그를 서비스 레포에 이슈카드로 생성

## Purpose

QA 테스트 과정에서 발견된 버그를 서비스 레포에 이슈카드로 생성하고, 바로 수정 작업을 시작할 수 있도록 브랜치명을 제공합니다.

## When to Use

| 트리거 | 설명 |
|--------|------|
| `버그 리포트`, `버그 발견` | QA 중 버그 발견 시 |
| `테스트 실패 → 버그 등록` | 테스트 케이스 실패 후 버그 리포팅 |
| `{서비스}에 버그 있어` | 특정 서비스 버그 리포팅 |

## Quick Start

```bash
# 버그 이슈 생성
gh issue create \
  --repo semicolon-devteam/{service-repo} \
  --title "[Bug] {버그 제목}" \
  --body "$(cat <<'EOF'
{이슈 본문}
EOF
)" \
  --label "bug,qa-found"
```

## Workflow

### Step 1: 정보 수집

```markdown
[SEMO] Skill: report-bug 호출

## 🐛 QA 버그 리포트 생성

다음 정보를 알려주세요:

1. **어떤 서비스에서 발생했나요?** (예: app-client, app-server)
2. **관련 테스트 케이스** (있다면)
3. **버그 요약** (제목으로 사용됨)
4. **재현 단계**
5. **기대 동작 vs 실제 동작**
6. **심각도** (critical/high/medium/low)
7. **스크린샷/로그** (있다면)

자유롭게 설명해주시면 정리해드릴게요.
```

### Step 2: 이슈 생성

```bash
gh issue create \
  --repo semicolon-devteam/{service-repo} \
  --title "[Bug] {버그 요약}" \
  --body "$(cat <<'EOF'
## 🐛 QA 버그 리포트

### 관련 테스트 케이스
- TC-{번호}: {테스트 케이스명}

### 요약
{버그 요약 설명}

### 재현 단계
1. {단계 1}
2. {단계 2}
3. {단계 3}

### 기대 동작
{원래 동작해야 하는 방식}

### 실제 동작
{현재 동작하는 방식}

### 심각도
- [x] {severity}

### 환경
- 테스트 환경: {STG/DEV}
- 발생 위치: {위치}
- 발견자: QA

### 증거
{스크린샷, 로그 등}

## 🌿 Branch

`fix/{issue-number}-{slug}`

---
🤖 SEMO report-bug Skill로 자동 생성됨 (QA)
EOF
)" \
  --label "bug,qa-found"
```

### Step 3: 완료 메시지

```markdown
[SEMO] Bug Report: 이슈 생성 완료

✅ QA 버그 리포트가 등록되었습니다!

**이슈**: semicolon-devteam/{service-repo}#{이슈번호}
**제목**: [Bug] {버그 제목}
**심각도**: {severity}
**테스트 케이스**: TC-{번호}

## 🌿 작업 브랜치

브랜치를 생성하려면:

\`\`\`bash
git checkout -b fix/{이슈번호}-{slug}
\`\`\`

개발자에게 할당하시겠어요?
```

## Issue Template

```markdown
## 🐛 QA 버그 리포트

### 관련 테스트 케이스
- TC-{번호}: {테스트 케이스명}
- 테스트 유형: {수동/자동}

### 요약
{버그에 대한 간단한 설명}

### 재현 단계
1. {환경/페이지로 이동}
2. {특정 동작 수행}
3. {버그 발생}

### 기대 동작
{정상적으로 동작해야 하는 방식}

### 실제 동작
{현재 발생하는 문제}

### 심각도
- [ ] Critical - 서비스 불가
- [ ] High - 주요 기능 장애
- [ ] Medium - 기능 일부 문제
- [ ] Low - 사소한 이슈

### 환경
- 테스트 환경: STG / DEV
- 발생 위치: {페이지/API 경로}
- 관련 기능: {기능명}
- 브라우저/디바이스: {해당 시}

### 증거
- 스크린샷: {첨부}
- 에러 로그: {첨부}
- 콘솔 로그: {첨부}

## 🌿 Branch

`fix/{issue-number}-{slug}`

---
🤖 SEMO report-bug Skill로 자동 생성됨 (QA)
```

## QA 특화 기능

### 테스트 케이스 연동

테스트 케이스 번호가 있으면 자동으로 연결:

```markdown
### 관련 테스트 케이스
- TC-042: 로그인 - 정상 케이스
- 결과: ❌ FAIL
```

### 테스트 결과 연동

`report-test-result` Skill과 연동하여 실패한 테스트에서 바로 버그 리포트 생성 가능:

```markdown
[SEMO] 테스트 결과: 3/10 실패

실패한 테스트에서 버그 리포트를 생성하시겠어요?
> "TC-042 버그 등록해줘"
```

## SEMO Message Format

```markdown
[SEMO] Skill: report-bug 사용

[SEMO] Bug Report: {repo}#{number} 생성 완료 → fix/{number}-{slug}
```

## Related

- [report-test-result Skill](../report-test-result/SKILL.md) - 테스트 결과 리포팅
- [execute-test Skill](../execute-test/SKILL.md) - 테스트 실행
- [iteration-tracker Skill](../iteration-tracker/SKILL.md) - QA 이터레이션 추적

## References

- [Issue Template](references/issue-template.md) - QA 버그 리포트 템플릿 상세
- [Severity Guide](references/severity-guide.md) - 심각도 기준
