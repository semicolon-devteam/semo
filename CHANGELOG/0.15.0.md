# 0.15.0 (2025-12-03)

## Fixed

- `sax-architecture-checker` 중첩 심링크 경로 오류 수정
- `ensure_real_directory()` 함수 추가: 심링크 디렉토리를 실제 디렉토리로 변환
- 심링크 생성 후 접근 가능 여부 검증 로직 추가

## Changed

- `fix_merged_dir()`: 심링크 디렉토리 감지 및 실제 디렉토리 변환 로직 추가
- `fix_commands()`: 동일한 심링크 디렉토리 처리 로직 추가
- `ln -s` → `ln -sfn` 변경 (기존 심링크 강제 덮어쓰기)
- 패키지 감지에 `design` 추가

## 해결된 이슈

- Fixes #2: sax-core 컴포넌트 병합 시 심링크 경로 오류 - 중첩 심링크 미고려
