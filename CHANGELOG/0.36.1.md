# 0.36.1 (2025-12-02)

## Fixed

- **install-sax.sh `--update` 모드 버그 수정**
  - 업데이트 시 심링크 재설정이 실행되지 않던 문제 수정
  - `return 1` → `return 2`로 변경하여 업데이트 완료 상태 구분
  - `setup_symlinks()`가 신규 설치와 업데이트 모두에서 실행되도록 수정

## Notes

- 이 수정 이전에 `--update`로 업데이트한 환경에서는 commands가 병합되지 않음
- 수정 후 다시 `./install-sax.sh [package] --update` 실행 필요
