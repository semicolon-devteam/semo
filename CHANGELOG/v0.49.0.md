# v0.49.0

> 2025-12-10

## Added

### install-sax.sh - _shared 심링크 지원
- `setup_shared_link()` 함수 추가
- `.claude/_shared → sax-core/_shared` 심링크 자동 생성
- Windows 환경에서는 복사 모드로 동작
- 설치 완료 요약 메시지에 `_shared` 구조 표시

## Changed

### deploy-workflow.md 업데이트
- 배포 구조에 `_shared` 심링크 포함
- 심링크 기반 참조 경로 설명 추가

## Notes

이 변경과 함께 sax-core v0.28.0에서:
- `sax-architecture-checker`에 `_shared` 심링크 검증 추가
- 8개 패키지 CLAUDE.md의 참조 경로를 `../_shared/`로 표준화
