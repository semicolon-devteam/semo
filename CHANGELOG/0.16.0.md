# 0.16.0 (2025-12-03)

## Added

- `install-sax.sh`에 다중 패키지 감지 기능 추가: `detect_existing_packages()`
- `install-sax.sh`에 다중 패키지 처리 로직 추가: `handle_multiple_packages()`
  - 기존 패키지 자동 감지 및 제거 확인
  - 단일 패키지 정책 강제
- `install-sax.sh`에 설치 후 자동 검증 추가: `verify_installation()`
  - 깨진 심링크 체크
  - CLAUDE.md 존재 확인
  - .merged 마커 검증
  - 심링크 개수 검증
- `sax-architecture-checker`에 다중 패키지 감지 섹션 추가
  - 다중 패키지 설치 시 경고 메시지 출력
  - 해결 방법 안내

## Changed

- `sax-architecture-checker` 패키지 감지 로직 개선
  - 단일 패키지 감지 → 다중 패키지 감지로 변경
  - design 패키지 추가
- `fix-logic.md`에 `check_multiple_packages()` 함수 추가

## Resolves

- Closes #3: 다중 SAX 패키지 설치 충돌 해결
