---
name: /SAX:help
description: 대화형 도우미 - SAX 패키지 개발, 관리 프로세스, 다음 단계 안내
trigger: "/SAX:help"
---

# /SAX:help Command

SAX 개발자/관리자가 길을 잃었을 때 대화형으로 안내하는 SAX 도우미입니다.

## Trigger

- `/SAX:help` 명령어
- "도움말", "뭘 해야 하지", "길을 잃었어" 키워드

## Purpose

이 명령어는 다음 상황에서 SAX 개발자/관리자를 돕습니다:

1. **방향성 상실**: "다음에 뭘 해야 하지?"
2. **프로세스 질문**: "SAX 패키지 관리 워크플로우가 어떻게 되지?"
3. **작업 현황 확인**: "내가 어디까지 했더라?"
4. **개념 질문**: "Agent가 뭐야?", "Skill은 어떻게 만들어?"

## Action

`/SAX:help` 실행 시 대화형 질의응답 프로세스를 시작합니다.

### Step 1: 상황 파악

먼저 사용자의 현재 상황을 파악합니다:

```markdown
[SAX] Orchestrator: 의도 분석 완료 → 도움 요청

## 🤝 SAX 도우미 (패키지 개발자/관리자)

무엇을 도와드릴까요?

### 💭 질문 유형을 선택하세요

1. **📍 SAX 구조 이해**: "SAX 패키지 구조가 어떻게 되나요?"
2. **🔧 Agent/Skill/Command 관리**: "새 Agent를 추가하고 싶어요"
3. **📦 패키지 배포**: "패키지를 배포하고 싶어요"
4. **🔄 버저닝**: "버전 릴리스는 어떻게 하나요?"
5. **🛠️ 도구 사용법**: "각 매니저(Agent/Skill/Command)는 뭐가 달라요?"
6. **❓ 기타**: "다른 질문이 있어요"

번호를 선택하거나 자유롭게 질문해주세요.
```

### Step 2: 맞춤형 응답

사용자 선택에 따라 적절한 정보를 제공합니다.

#### 1️⃣ SAX 구조 이해

```markdown
## 📋 SAX 패키지 구조 가이드

### 전체 구조

```text
SAX-Core (공통 규칙)
    ↓ 상속
SAX-Meta (메타 관리)
    ↓ 관리
SAX-PO, SAX-Next, SAX-Spring (도메인 패키지)
```

### 패키지 컴포넌트

| 컴포넌트 | 역할 | 위치 |
|----------|------|------|
| **Agent** | 복잡한 멀티스텝 작업 수행 | `agents/` |
| **Skill** | 단일 목적의 재사용 가능 기능 | `skills/` |
| **Command** | 사용자 명시적 트리거 | `commands/SAX/` |

### 핵심 원칙

1. **세션 컨텍스트 비의존**: 모든 정보는 Reference Chain 내에서 접근 가능
2. **중복 체크 필수**: 생성/수정 전 반드시 기존 문서 확인
3. **서브모듈 동기화**: sax-meta 수정 시 .claude/ 동기화 필수

**더 알고 싶은 부분이 있나요?**
```

#### 2️⃣ Agent/Skill/Command 관리 (매니저 활용)

```markdown
[SAX] Agent: {manager} 호출 (트리거: 컴포넌트 관리 요청)

## 🔧 컴포넌트 관리 가이드

### 어떤 컴포넌트를 관리하시겠어요?

**Agent 관리**:
```bash
> "새 Agent 만들어줘"
```
→ agent-manager Agent가 처리

**Skill 관리**:
```bash
> "새 Skill 추가해줘"
```
→ skill-manager Agent가 처리

**Command 관리**:
```bash
> "새 Command 만들어줘"
```
→ command-manager Agent가 처리

### 각 매니저의 역할

| 매니저 | 역할 | 트리거 |
|--------|------|--------|
| agent-manager | Agent 라이프사이클 관리 | Agent 관련 요청 |
| skill-manager | Skill 라이프사이클 관리 | Skill 관련 요청 |
| command-manager | Command 라이프사이클 관리 | Command 관련 요청 |

**지금 어떤 컴포넌트를 만들어볼까요?** (Y/n)
```

#### 3️⃣ 패키지 배포 (package-deploy Skill 활용)

```markdown
[SAX] Skill: package-deploy 사용

## 📦 패키지 배포 가이드

### 배포 대상

| 패키지 | 대상 | 배포 방법 |
|--------|------|----------|
| sax-po | 외부 프로젝트 | submodule 설치 |
| sax-next | 외부 프로젝트 | submodule 설치 |
| sax-meta | docs 레포 | 직접 동기화 |

### 배포 명령

**새 프로젝트에 SAX 설치**:
```bash
> "이 프로젝트에 SAX 설치해줘"
```
→ package-deploy Skill이 처리

**기존 SAX 업데이트**:
```bash
> "SAX 업데이트해줘"
```
→ 서브모듈 업데이트 실행

### 설치 스크립트

```bash
# GitHub CLI 사용 (권장)
gh api repos/semicolon-devteam/sax-meta/contents/scripts/install-sax.sh \
  --jq '.content' | base64 -d > install-sax.sh
chmod +x install-sax.sh
./install-sax.sh {po|next|meta}
```

**지금 배포를 진행할까요?** (Y/n)
```

#### 4️⃣ 버저닝 (version-manager Skill 활용)

```markdown
[SAX] Skill: version-manager 사용

## 🔄 버저닝 가이드

### 버전 타입

| 변경 유형 | 버전 타입 | 예시 |
|----------|----------|------|
| Agent/Skill/Command 추가 | MINOR | 0.1.0 → 0.2.0 |
| Agent/Skill/Command 수정 | MINOR | 0.2.0 → 0.3.0 |
| 버그/오타 수정 | PATCH | 0.2.0 → 0.2.1 |
| Breaking Change | MAJOR | 0.2.0 → 1.0.0 |

### 버저닝 명령

```bash
> "버저닝 해줘"
> "릴리스해줘"
```

### 버저닝 프로세스

1. VERSION 파일 업데이트
2. CHANGELOG/{version}.md 생성
3. 표준 커밋 메시지로 커밋
4. GitHub에 푸시
5. 서브모듈 동기화

**지금 버저닝을 진행할까요?** (Y/n)
```

#### 5️⃣ 도구 사용법

```markdown
## 🛠️ SAX 도구 사용법

### 매니저 Agent들

**agent-manager**:
- Agent 생성, 수정, 삭제
- Agent 템플릿 관리
- Reference Chain 설정

**skill-manager**:
- Skill 생성, 수정, 삭제
- Skill 디렉토리 구조 관리
- SKILL.md 템플릿 관리

**command-manager**:
- Command 생성, 수정, 삭제
- 파일 위치: `commands/SAX/{name}.md`
- CLAUDE.md Commands 섹션 업데이트

### Skills

**package-validator**:
- SAX 패키지 구조 검증
- Sub-Agent 최적화 규칙 검사

**version-manager**:
- VERSION 파일 관리
- CHANGELOG 생성
- 시맨틱 버저닝

**package-sync**:
- 패키지 소스 → .claude/ 동기화
- docs 레포 한정

**package-deploy**:
- 외부 프로젝트 SAX 배포
- submodule 설치 자동화

**더 알고 싶은 도구가 있나요?**
```

#### 6️⃣ 기타 질문

```markdown
## ❓ 자유 질문

궁금한 점을 자유롭게 물어보세요.

**예시 질문**:
- "패키지 구조 템플릿은 어떻게 되나요?"
- "여러 패키지에 동시에 작업할 수 있나요?"
- "Core 원칙은 어디서 확인하나요?"
- "에러가 발생하면 어떻게 피드백하나요?"

**답변 방식**:
- 간단한 질문: 직접 답변
- 복잡한 질문: 적절한 Agent/Skill 호출
- 학습이 필요한 질문: 관련 문서 안내
```

### Step 3: 추가 질문 유도

답변 후 항상 추가 질문을 유도합니다:

```markdown
---

**도움이 되셨나요?**

다른 궁금한 점이 있으시면 언제든 물어보세요:
- "Agent 만들어줘" → agent-manager 호출
- "Skill 추가해줘" → skill-manager 호출
- "/SAX:help" → 도움말 처음으로

**단축키**:
- `/SAX:feedback` - SAX 관련 피드백
- `skill:version-manager` - 버저닝
- `skill:package-validator` - 패키지 검증
```

## Integration with Other Tools

### agent-manager Agent

Agent 관리 요청 시 위임:

```markdown
User: "새 Agent 만들어줘"

[SAX] Orchestrator: /SAX:help 트리거

[SAX] Agent: agent-manager 호출 (트리거: Agent 생성 요청)
→ Agent 템플릿 생성
→ Reference Chain 설정
→ CLAUDE.md 업데이트
```

### skill-manager Agent

Skill 관리 요청 시 위임:

```markdown
User: "새 Skill 추가해줘"

[SAX] Orchestrator: /SAX:help 트리거

[SAX] Agent: skill-manager 호출 (트리거: Skill 생성 요청)
→ Skill 디렉토리 생성
→ SKILL.md 템플릿 생성
→ CLAUDE.md 업데이트
```

### command-manager Agent

Command 관리 요청 시 위임:

```markdown
User: "새 Command 만들어줘"

[SAX] Orchestrator: /SAX:help 트리거

[SAX] Agent: command-manager 호출 (트리거: Command 생성 요청)
→ commands/SAX/{name}.md 생성
→ CLAUDE.md Commands 섹션 업데이트
```

### version-manager Skill

버저닝 요청 시 호출:

```markdown
User: "버전 릴리스해줘"

[SAX] Orchestrator: /SAX:help 트리거

[SAX] Skill: version-manager 사용
→ VERSION 파일 업데이트
→ CHANGELOG 생성
→ 커밋 및 푸시
```

## Expected Output

```markdown
[SAX] Orchestrator: 의도 분석 완료 → 도움 요청

## 🤝 SAX 도우미 (패키지 개발자/관리자)

무엇을 도와드릴까요?

### 💭 질문 유형을 선택하세요

1. **📍 SAX 구조 이해**: "SAX 패키지 구조가 어떻게 되나요?"
2. **🔧 Agent/Skill/Command 관리**: "새 Agent를 추가하고 싶어요"
3. **📦 패키지 배포**: "패키지를 배포하고 싶어요"
4. **🔄 버저닝**: "버전 릴리스는 어떻게 하나요?"
5. **🛠️ 도구 사용법**: "각 매니저(Agent/Skill/Command)는 뭐가 달라요?"
6. **❓ 기타**: "다른 질문이 있어요"

번호를 선택하거나 자유롭게 질문해주세요.
```

## Critical Rules

1. **대화형 접근**: 사용자가 질문을 선택하거나 자유롭게 물어볼 수 있도록
2. **SAX 개발자 맥락 유지**: 패키지 개발/관리 관점의 응답
3. **단계적 안내**: 한 번에 한 단계씩, 명확하게
4. **추가 질문 유도**: 항상 "더 궁금한 점?" 질문
5. **Agent/Skill 위임**: 복잡한 작업은 적절한 Agent/Skill로 위임

## References

- [agent-manager Agent](../agents/agent-manager/agent-manager.md)
- [skill-manager Agent](../agents/skill-manager/skill-manager.md)
- [command-manager Agent](../agents/command-manager/command-manager.md)
- [version-manager Skill](../skills/version-manager/SKILL.md)
- [package-deploy Skill](../skills/package-deploy/SKILL.md)
- [SAX Core PRINCIPLES.md](https://github.com/semicolon-devteam/docs/blob/main/sax/core/PRINCIPLES.md)
