name: Publish semo-mcp

on:
  push:
    tags:
      - 'mcp-v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 2.1.0)'
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/mcp-server

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Generate encrypted tokens
        env:
          SEMO_SLACK_BOT_TOKEN: ${{ secrets.SEMO_SLACK_BOT_TOKEN }}
          SEMO_GITHUB_APP_TOKEN: ${{ secrets.SEMO_GITHUB_APP_TOKEN }}
          SEMO_DB_PASSWORD: ${{ secrets.SEMO_DB_PASSWORD }}
        run: node scripts/generate-tokens.js

      - name: Build
        run: npm run build

      - name: Update version (if manual dispatch)
        if: github.event_name == 'workflow_dispatch'
        run: npm version ${{ github.event.inputs.version }} --no-git-tag-version --allow-same-version

      - name: Publish to npm
        id: publish
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Job Summary (Success)
        if: success()
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "## ✅ semo-mcp v${VERSION} 배포 성공" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 패키지 정보" >> $GITHUB_STEP_SUMMARY
          echo "| 항목 | 값 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-----|" >> $GITHUB_STEP_SUMMARY
          echo "| 패키지 | \`@team-semicolon/semo-mcp\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 버전 | \`${VERSION}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 트리거 | \`${{ github.event_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 커밋 | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔐 토큰 상태" >> $GITHUB_STEP_SUMMARY
          echo "| 토큰 | 상태 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| SEMO_SLACK_BOT_TOKEN | ✅ 암호화됨 |" >> $GITHUB_STEP_SUMMARY
          echo "| SEMO_ENCRYPTION_KEY | ✅ 설정됨 |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📥 설치 방법" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "npx @team-semicolon/semo-mcp@${VERSION}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔗 링크" >> $GITHUB_STEP_SUMMARY
          echo "- [npm 패키지](https://www.npmjs.com/package/@team-semicolon/semo-mcp)" >> $GITHUB_STEP_SUMMARY

      - name: Job Summary (Failure)
        if: failure()
        run: |
          VERSION=$(node -p "require('./package.json').version" 2>/dev/null || echo "unknown")
          echo "## ❌ semo-mcp 배포 실패" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 실패 정보" >> $GITHUB_STEP_SUMMARY
          echo "| 항목 | 값 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-----|" >> $GITHUB_STEP_SUMMARY
          echo "| 버전 | \`${VERSION}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 트리거 | \`${{ github.event_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 커밋 | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔍 체크리스트" >> $GITHUB_STEP_SUMMARY
          echo "- [ ] \`SEMO_ENCRYPTION_KEY\` 시크릿 설정 확인 (32자)" >> $GITHUB_STEP_SUMMARY
          echo "- [ ] \`SEMO_SLACK_BOT_TOKEN\` 시크릿 설정 확인" >> $GITHUB_STEP_SUMMARY
          echo "- [ ] \`NPM_TOKEN\` 시크릿 설정 확인" >> $GITHUB_STEP_SUMMARY
          echo "- [ ] npm 패키지 버전 중복 확인" >> $GITHUB_STEP_SUMMARY

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          name: semo-mcp ${{ github.ref_name }}
          body: |
            ## semo-mcp ${{ github.ref_name }}

            SEMO MCP Server 업데이트

            ### 설치/업데이트
            ```bash
            npx @team-semicolon/semo-mcp@latest
            ```

            ### Changes
            - 팀 공용 토큰 암호화 내장
            - 보안 강화
          draft: false
          prerelease: false
