---
name: current-tasks
description: |
  현재 테스트 대기 중인 업무 리스트 확인 및 진행 상황 보고. Use when:
  (1) "현재 업무 확인", (2) "지금 뭐 테스트해야 돼?", (3) "테스트 대기 목록",
  (4) "내 할 일", (5) `/SAX:current-tasks` 명령어 입력
tools:
  - run_command
  - read_file
---

> **🔔 시스템 메시지**: 이 Skill이 호출되면 `[SAX] Skill: current-tasks 실행` 시스템 메시지를 첫 줄에 출력하세요.

# current-tasks Skill

> 현재 테스트 대기 중인 업무 리스트를 확인하고 진행 상황을 보고합니다.

## Purpose

GitHub Projects '이슈관리' 프로젝트(ID: 1)에서 '테스트중' 상태인 이슈를 조회하고, 각 이슈의 QA 테스트 체크리스트 진행 상황을 보고합니다.

## Trigger

- "현재 업무 확인"
- "지금 뭐 테스트해야 돼?"
- "테스트 대기 목록"
- "내 할 일"
- `/SAX:current-tasks` 명령어

## Workflow

### 1. '테스트중' 상태 이슈 조회

```bash
# GitHub CLI로 프로젝트 보드 조회
gh project item-list 1 --owner semicolon-devteam --format json \
  | jq '.items[] | select(.status == "테스트중") | {number: .content.number, title: .content.title, url: .content.url}'
```

### 2. 각 이슈의 테스트 체크리스트 확인

각 이슈에 대해:

```bash
# 이슈 본문 가져오기
gh issue view {issue_number} --json body --jq '.body'
```

### 3. QA 테스트 섹션 파싱

이슈 본문에서 `## QA 테스트` 섹션을 찾아 체크리스트 분석:

```markdown
## QA 테스트

- [x] 로그인 기능 테스트
- [ ] 회원가입 기능 테스트
- [ ] 비밀번호 재설정 테스트
```

### 4. 결과 보고

```markdown
[SAX] Skill: current-tasks 실행

# 📋 현재 테스트 업무

## 테스트중 이슈 ({총 개수}개)

### 1. #{issue_number} - {이슈 제목}

**URL**: {issue_url}

**QA 테스트 진행 상황**:
- ✅ 로그인 기능 테스트
- ⬜ 회원가입 기능 테스트 (미완료)
- ⬜ 비밀번호 재설정 테스트 (미완료)

**진행률**: 1/3 (33%)

---

### 2. #{issue_number} - {이슈 제목}
...

---

## 📊 전체 요약

- **총 테스트중 이슈**: {n}개
- **총 테스트 항목**: {m}개
- **완료된 항목**: {x}개
- **미완료 항목**: {y}개
- **전체 진행률**: {x/m * 100}%

## 💡 다음 액션

테스트를 시작하려면:
```
/SAX:run-test
```

특정 이슈 선택:
```
"#{issue_number} 테스트 시작"
```
```

## Implementation

### Step 1: 프로젝트 보드 조회

```bash
gh project item-list 1 --owner semicolon-devteam --format json
```

출력 예시:
```json
{
  "items": [
    {
      "content": {
        "number": 123,
        "title": "로그인 기능 구현",
        "url": "https://github.com/owner/repo/issues/123"
      },
      "status": "테스트중"
    }
  ]
}
```

### Step 2: 이슈별 체크리스트 파싱

```bash
gh issue view 123 --json body --jq '.body'
```

정규식으로 `## QA 테스트` 섹션 추출:
```
## QA 테스트\n(- \[[ x]\] .*\n)+
```

### Step 3: 체크 상태 계산

- `[x]` → 완료
- `[ ]` → 미완료

## Error Handling

### 프로젝트 접근 오류

```markdown
[SAX] Skill: current-tasks 실행

❌ **프로젝트 접근 실패**

프로젝트 ID 1('이슈관리')에 접근할 수 없습니다.
- GitHub CLI 인증을 확인해주세요: `gh auth status`
- 프로젝트 권한을 확인해주세요
```

### 이슈에 QA 테스트 섹션 없음

```markdown
### #{issue_number} - {제목}

⚠️ **QA 테스트 섹션이 없습니다**

이 이슈에 QA 테스트 체크리스트를 추가하려면:
```
"#{issue_number}에 테스트 케이스 추가해줘"
```
```

### 테스트중 이슈 없음

```markdown
[SAX] Skill: current-tasks 실행

# 📋 현재 테스트 업무

✅ **테스트 대기 중인 이슈가 없습니다!**

새 이슈를 '테스트중' 상태로 변경하려면:
```
"#{issue_number} 테스트 시작"
```
```

## Output Format

- SAX 메시지 규칙 준수
- 이슈 번호는 `#{number}` 형식
- URL은 마크다운 링크
- 체크 상태는 ✅/⬜ 이모지
- 진행률은 퍼센트로 표시

## Integration

### qa-master Agent

```markdown
User: "지금 뭐 테스트해야 돼?"
→ [SAX] Agent 위임: qa-master
→ qa-master가 current-tasks skill 호출
```

### test-queue Skill

current-tasks와 유사하지만:
- current-tasks: 전체 테스트중 이슈 개요
- test-queue: 특정 이슈의 상세 테스트 큐

## SAX Message Format

```markdown
[SAX] Skill: current-tasks 실행

# 📋 현재 테스트 업무
...
```

## Critical Rules

1. **프로젝트 ID는 하드코딩**: '이슈관리' 프로젝트 ID는 1
2. **상태 필터**: '테스트중' 상태만
3. **QA 테스트 섹션 필수**: 섹션 없으면 경고 표시
4. **체크리스트 파싱 정확도**: `- [x]`와 `- [ ]` 정확히 구분

## Related

- [qa-master Agent](../../agents/qa-master/qa-master.md) - QA 워크플로우 총괄
- [test-queue Skill](../test-queue/SKILL.md) - 테스트 큐 관리
- [execute-test Skill](../execute-test/SKILL.md) - 테스트 실행

## References

- [GitHub CLI Projects](references/gh-projects.md) - gh project 명령어
- [Issue Parsing](references/issue-parsing.md) - 이슈 본문 파싱
