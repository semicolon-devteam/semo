# /SEMO:permission-test

SEMO Remote 권한 요청 인터랙션 테스트를 위한 커맨드입니다.

## 용도

- SEMO Remote client-app 연동 테스트
- Claude Code 세션의 권한 요청 인터랙션 검증
- Mobile PWA에서 권한 승인/거부 플로우 테스트

## 동작

이 커맨드가 호출되면:

1. `PERMISSIONS_TEST.md` 파일 생성을 시도합니다
2. Claude Code가 자동으로 권한 요청 인터랙션을 표시합니다
3. SEMO Remote client-app에서 승인/거부를 처리할 수 있습니다

## 응답

```markdown
[SEMO] Remote Test: permission-test

🔐 권한 요청 인터랙션 테스트를 시작합니다.

**테스트 시나리오**: 파일 생성 권한 요청
- 파일: `PERMISSIONS_TEST.md`
- 예상 동작: Claude Code 권한 요청 UI 표시

아래에서 권한 요청이 표시됩니다...
```

## 실행

```bash
# 테스트 파일 생성 시도 (권한 요청 트리거)
```

그 후 `PERMISSIONS_TEST.md` 파일을 생성합니다:

```markdown
# SEMO Remote Permission Test

✅ 권한 테스트 성공!

- 테스트 시간: {timestamp}
- 세션: {session_id}

이 파일은 테스트 완료 후 삭제해도 됩니다.
```

## 완료 메시지

```markdown
[SEMO] Remote Test: permission-test 완료 ✅

| 항목 | 결과 |
|------|------|
| 권한 요청 | ✅ 표시됨 |
| 사용자 응답 | ✅ 승인됨 |
| 파일 생성 | ✅ 완료 |

테스트 파일: `PERMISSIONS_TEST.md`
```
