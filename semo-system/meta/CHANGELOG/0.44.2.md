# v0.44.2

> 2024-12-05

## Fixed

- `.gitignore`에 `.claude`가 포함된 프로젝트에서 설치 실패 문제 수정
  - `git submodule add`에 `-f` 플래그 추가
  - `.gitignore` 무시하고 서브모듈 강제 추가

## Context

- 일부 프로젝트에서 `.claude` 디렉토리가 `.gitignore`에 포함되어 있음
- 서브모듈은 `.gitignore`와 별개로 추적되어야 함
