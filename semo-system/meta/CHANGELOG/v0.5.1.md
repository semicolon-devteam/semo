# v0.5.1 (2024-11-27)

## Fixed

### Scripts

- **install-sax.sh git index 완전 삭제 버그 수정**
  - `git rm -rf --cached` 사용으로 강제 삭제
  - `git reset HEAD` 추가로 스테이징 영역 초기화
  - `git update-index --force-remove` 최종 안전장치 추가
  - 빈 `.gitmodules` 파일 자동 삭제 처리

## Changed

### Scripts

- **install-sax.sh 한글화**
  - 모든 메시지 한글로 변경
  - 사용법, 옵션 설명, 진행 상태 메시지 한글화
  - 설치 완료 안내 한글화
