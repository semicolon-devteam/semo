# 2026-03-10 작업 로그

## Star Spot 프로젝트 — 랜딩 페이지 재설계

### 15:30 - 기획서 작성 완료
- **요청**: Reus가 랜딩을 셀럽 리스트형으로 변경 요청
- **와이어프레임**: 3열 그리드 셀럽 리스트 (Search Bar + Filter Container)
- **기획서 경로**: `/Users/reus/.openclaw-planclaw/workspace/specs/star-spot/landing-page-redesign.md`

**주요 기획 내용**:
- 랜딩: 지도 뷰 → 셀럽 리스트 (3열 그리드)
- 카테고리: 배우 | 아이돌 | 스트리머 | 인플루언서
- 정렬: 인기순 (방문 맛집 수) | 가나다순 | 최신 등록순
- 필터 팝업: 바텀 시트 형태
- 바텀 네비: 홈 | 지도 | 검색 | MY

### 15:37 - GitHub 이슈 생성
- **레포**: `semicolon-devteam/proj-star-spot` (SemiClaw 안내)
- **이슈**: [#32](https://github.com/semicolon-devteam/proj-star-spot/issues/32)
- **라벨**: `bot:spec-ready` (WorkClaw 픽업 대기)
- **프로젝트 보드**: 등록 완료 (프로젝트 #1)

**다음 단계**: WorkClaw이 5분 폴링으로 이슈 감지 → 구현 시작

---

**교훈**: 레포 이름을 모를 때 → SemiClaw(<@U0ADGB42N79>)에게 문의 (추측 금지)

---

## SEMO `/bots` 페이지 — 빈 화면 이슈 해결

### 18:24 - Reus 문의
- **현상**: `https://semo.semi-colon.space/bots` 페이지가 빈 화면으로 표시됨
- **원인 파악**: SemiClaw 조사 결과
  - 코드는 구현됨 (`/api/bots` → `semo.bot_status` 테이블 조회)
  - `semo.bot_status` 테이블에 데이터가 없어서 빈 페이지
  - 봇 상태 sync 워커가 구현 안 됨

### 18:39 - GitHub 이슈 생성
**생성된 이슈:**
1. **#151** — [DB] semo.bot_status 테이블 스키마 정의 및 생성
   - URL: https://github.com/semicolon-devteam/semo/issues/151
   - 라벨: `enhancement,bot:spec-ready`
   - 내용: 마이그레이션 파일 생성 + 테이블 스키마 정의
2. **#152** — [Feature] 봇 상태 동기화 워커 구현
   - URL: https://github.com/semicolon-devteam/semo/issues/152
   - 라벨: `enhancement,bot:spec-ready`
   - 의존성: #151 완료 후 진행
   - 내용: 각 봇 gateway에서 5분마다 상태 SEMO DB로 전송

**프로젝트 보드 등록 실패:**
- GitHub token에 `read:project` scope 부족
- Reus에게 수동 등록 가이드 제공함

**다음 단계**: WorkClaw이 5분 폴링으로 두 이슈 감지 → 구현 시작

### 21:58 - Reus 재확인: 여전히 빈 페이지
**문제 진단:**
- #151 (DB 스키마) → PR #153 **머지 완료** (2026-03-10 12:21)
- #152 (봇 상태 sync 워커) → `bot:blocked` 상태로 **방치됨**
  - 블로커 사유: "#151 완료 안 됨" (잘못된 판단)
  - 실제: #151은 이미 완료됨
- 빈 페이지 이유: DB 테이블은 생성되었지만 **데이터가 없음**

**조치:**
- #152 라벨 변경: `bot:blocked` → `bot:spec-ready`
- WorkClaw이 재픽업 예정

**다음 단계**: #152 완료되면 각 봇이 5분마다 상태 전송 → `/bots` 페이지에 봇 팀 현황 표시

### 22:00 - Reus 피드백: DB 비어있어도 봇 목록은 보여야 함
**문제 진단:**
- 현재 코드: DB에 행이 없으면 빈 배열 반환
- Fallback이 "메타데이터 보강" 수준에서만 작동 (DB 행이 있을 때)
- "봇 발견" 수준에서는 작동 안 함 (DB 비어있을 때 GitHub 스캔 안 함)

**생성된 이슈:**
- **#154** — [Bug] /api/bots returns empty array when DB is empty (missing GitHub fallback)
  - URL: https://github.com/semicolon-devteam/semo/issues/154
  - 라벨: `bug,bot:spec-ready`
  - 수정: DB 비어있을 때 GitHub `semo-system/bot-workspaces/` 디렉토리 스캔 + IDENTITY.md 파싱

**프로젝트 보드**: GitHub scope 부족으로 수동 등록 필요

**다음 단계**: WorkClaw이 #154 감지 → 구현

---

## SEMO `/kb` 페이지 — 404 에러

### 22:14 - ReviewClaw 요청 (via Reus)
**문제:**
- `layout.tsx`에 Knowledge Base 링크(`/kb`)는 있음
- 실제 페이지 파일(`app/kb/page.tsx`)이 없어서 404 발생

**생성된 이슈:**
- **#156** — [Feature] /kb 페이지 구현 (Knowledge Base 관리 UI)
  - URL: https://github.com/semicolon-devteam/semo/issues/156
  - 라벨: `enhancement,bot:spec-ready`
  - 기능:
    - KB 엔트리 조회/검색 (벡터 검색 지원 선택사항)
    - 엔트리 추가/수정/삭제
    - 봇별/카테고리별/태그별 필터링
    - `/api/kb` API 라우트 구현

**프로젝트 보드**: GitHub scope 부족으로 수동 등록 필요

**다음 단계**: WorkClaw이 #156 감지 → 구현

---

## proj-acaiv — Phase 6: 오토파일럿 (완전 자동화) 기획서 작성

### 18:42 - 기획서 작성 완료
- **레포**: `semicolon-devteam/proj-acaiv`
- **이슈**: [#7](https://github.com/semicolon-devteam/proj-acaiv/issues/7)
- **내용**: 주제 입력 → Phase 1~5 자동 실행 + 실시간 진행 상황 UI
- **기술 스택**: BullMQ (작업 큐) + Socket.io (실시간 통신)
- **라벨 전환**: `bot:needs-spec` → `bot:spec-ready`

**주요 기획 내용**:
- 설정 페이지: 포맷/스타일/음성/길이 프리셋 저장
- 오토파일럿 페이지: 주제 입력 → 전 단계 자동 실행
- 실시간 UI: Socket.io로 진행률 표시 (0-100%)
- 중단/재시도 기능
- DB: `autopilot_presets`, `autopilot_jobs` 테이블

**다음 단계**: WorkClaw이 5분 폴링으로 감지 → 구현 시작
