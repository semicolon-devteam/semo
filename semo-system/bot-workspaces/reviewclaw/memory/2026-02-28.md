# 2026-02-28 활동 로그 — ReviewClaw 🔍

## 21:48 proj-play-land #120 리뷰

### 요청
- SemiClaw로부터 리뷰 요청: proj-play-land #120 "아이돌 신청 거절 후 재신청 기능 추가"
- 리뷰 대기 이슈 1건 + 리뷰 없는 PR 2건 (PR #122, #121)

### 발견 사항
- **중복 PR**: PR #122 vs #121 (같은 기능, 다른 구현 방식)
  - PR #122: URL 쿼리 파라미터 방식 (`?reapply=true`)
  - PR #121: 상태 관리 방식 + 대폭 리팩토링 (-93 라인)

### 리뷰 결과

#### PR #122 (feat/120-idol-reapply)
🔴 Must Fix:
- 중복 PR 해결 필요
- IdolAdmin bio 표시 추가 → scope creep (이슈 범위 외)
- `useSearchParams` SSR 이슈 (Suspense boundary 필요)

🟡 Should Fix:
- 재신청 후 `isReapplying` 상태 초기화 누락

🟢 Suggestion:
- `rejectReason` 타입 안전성 체크

#### PR #121 (120-idol-reapply)
🔴 Must Fix:
- 중복 PR 해결 필요
- `socialLinks`, `profileImageUrl` 삭제 → 백엔드 API 확인 필요
- `ApplicationForm` import 누락

🟡 Should Fix:
- 재신청 후 `isReapplying` 상태 초기화 누락
- `showReapply` 상태 관리 개선

🟢 Suggestion:
- 코드 리팩토링 칭찬 (props 제거, 불필요한 로직 제거)

### 액션
1. ✅ 이슈 #120에 중복 PR 상황 코멘트
2. ✅ PR #122, #121에 리뷰 코멘트 (Request Changes)
3. ✅ 라벨 변경: `bot:needs-review` → `bot:blocked` (사람 개입 필요)
4. ✅ #bot-ops에 리뷰 결과 공유 (proj-play-land는 매핑 테이블에 없어 기본값 사용)

### 인계
- <@URSQYUNQJ>에게 PR #122 vs #121 중 선택 요청
- 선택 후 나머지 PR close 필요
