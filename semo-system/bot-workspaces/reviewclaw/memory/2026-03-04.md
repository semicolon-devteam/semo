# 2026-03-04 활동 로그 — ReviewClaw 🔍

## 17:46 core-backend PR #230 리뷰

### 요청
- WorkClaw로부터 리뷰 요청: core-backend PR #230 "feat(#302): 오피스 배너 노출 개수/순서 관리 기능 추가 (백엔드)"
- 연결 이슈: proj-office-land #302

### 변경 사항 요약
- DB 스키마: `site_info.office_banner_limit`, `posts.display_order` 추가
- 정렬 로직: office-banner metadataType → `display_order NULLS LAST, id DESC`
- 신규 API: `PATCH /rest/v1/posts/office-banner/reorder`
- 파일: 7개 (마이그레이션, Repository, Service, Controller, DTO×2)

### 리뷰 결과

#### 🔴 Must Fix (2건)
1. **보안: postId office-banner 검증 누락**
   - 위치: `PostCommandService.reorderOfficeBanners`
   - 문제: 관리자 권한만 체크, metadataType 검증 안 함 → 임의 게시물에 display_order 설정 가능
   - 제안: `findAllById` + metadataType 필터링

2. **성능: Reactive에서 순차 awaitSingle 반복**
   - 위치: `PostCommandService.reorderOfficeBanners`
   - 문제: `forEachIndexed` + `awaitSingle` → DB N번 왕복
   - 제안: `Flux.fromIterable().index().flatMap()` 병렬 처리

#### 🟡 Should Fix (3건)
3. 중복 postId 방어 (Request DTO 또는 Service)
4. updatedCount 불일치 처리 (실패 postId 반환 또는 로그)
5. display_order 인덱스 최적화 (복합 인덱스 검토)

#### 🟢 Suggestion (1건)
6. nullable displayOrder 지원 (NULL 리셋 기능 대비)

### 액션
1. ✅ PR #230에 리뷰 코멘트 (self-PR이라 GitHub 리뷰 시스템 사용 불가)
2. ✅ 이슈 #302 라벨 변경: `bot:needs-review` → `bot:blocked`
3. ✅ 이슈 #302에 작업 로그 코멘트
4. ✅ #bot-ops에 리뷰 결과 공유 (proj-office-land는 매핑 테이블에 없어 기본값 사용)

### 인계
- WorkClaw에게 Must Fix 2건 수정 요청
- 수정 후 재리뷰 → approve → 라벨 `bot:done`
- 프론트엔드 PR은 백엔드 머지 후 진행 예정

### 특이사항
- self-PR로 인해 `gh pr review --request-changes` 실패 → PR 코멘트로 대체
- GitHub 제약: 자기 PR에 대한 공식 리뷰 불가

---

## 17:51 core-backend PR #230 재리뷰

### 요청
- WorkClaw로부터 재리뷰 요청 (커밋 369792f)
- Must Fix 2건 수정 완료 주장

### 수정 내용 확인
1. ✅ **보안: office-banner 검증**
   - `findAllById(postIds)` 조회
   - `metadata.type`이 "office-banner"인지 확인
   - 아니면 `InvalidRequest` 예외

2. ✅ **성능: 병렬 업데이트**
   - `Flux.fromIterable(postIds.withIndex())`
   - `.flatMap { ... }` 병렬 처리
   - 순차 awaitSingle 제거

3. ✅ **PostException.InvalidRequest 타입 추가**

### 재리뷰 결과
- **Must Fix 2건**: ✅ 모두 해결
- **Should Fix**: Optional이므로 approve
- **추가 개선 제안** (Optional):
  1. 존재하지 않는 postId 감지 (요청 3개인데 DB에 2개만 있는 경우)
  2. 중복 postId 방어 (Should Fix #3 미해결)

### 액션
1. ✅ PR #230 approve 코멘트
2. ✅ 이슈 #302 라벨 변경: `bot:blocked` → `bot:done`
3. ✅ 이슈 #302에 작업 로그 코멘트
4. ✅ #bot-ops에 approve 결과 공유

### 결론
- 백엔드 구현 완료 → 머지 가능
- 프론트엔드 PR 진행 대기

---

## 17:55 proj-office-land PR #305 리뷰

### 요청
- WorkClaw로부터 리뷰 요청: proj-office-land PR #305 "feat(#302): 오피스 배너 노출 개수 DB 설정 연동 (프론트엔드)"
- 연결 이슈: proj-office-land #302
- 백엔드 PR #230은 이미 머지 완료

### 변경 사항 요약
- 파일 2개: `useHomeData.ts`, `site-config.types.ts`
- +6 -1 (매우 간단)
- `SiteInformations` 타입에 `office_banner_limit?: number` 추가
- `useHomeData.ts`: `size: 50` → `siteInfo?.office_banner_limit ?? 50`

### 리뷰 결과
- **Must Fix**: 없음
- **Should Fix**: 성능 최적화 (중복 요청 방지) — optional
- **칭찬**: 백엔드 API와 정확히 일치, fallback 안전

### Optional 개선 제안
1. **중복 요청 방지 (성능)**
   - 현재: siteInfo undefined → bannerLimit=50 요청 → siteInfo 로딩 → bannerLimit=DB값 재요청
   - 제안: `useGetPostsQuery`에 `enabled: siteInfo !== undefined` 추가
   - 판단: Phase 1 목표는 달성, 성능 최적화는 별도 이슈로

### 액션
1. ✅ PR #305 approve 코멘트
2. ✅ 이슈 #302 라벨 정리 (`bot:needs-review` 제거)
3. ✅ 이슈 #302에 Phase 1 완료 코멘트
4. ✅ #bot-ops에 결과 공유

### Phase 1 완료
- ✅ 노출 개수 DB 설정 (백엔드 #230 + 프론트 #305)
- ⏳ 순서 수동 변경 (Phase 2 예정)

### 결론
- 프론트엔드 구현 완료 → 머지 가능
- 개발서버 배포 후 E2E 테스트 진행
- Phase 2 (드래그&드롭 UI + display_order API) 별도 진행
