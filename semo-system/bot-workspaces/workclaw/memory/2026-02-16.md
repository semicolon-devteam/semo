# 2026-02-16

## 플레이아이돌 테스트 워크플로우 설정 (23:30 KST, 2/15)

### 완료 항목
- 서브에이전트로 이슈 #2, #3, #4, #5, #7, #12에 엣지케이스 포함 테스트케이스 코멘트 작성 요청
- `memory/play-idol-test-workflow.md` 생성 — PR→자가리뷰→머지→GH Actions 배포확인→개발환경 E2E 전체 워크플로우 저장
- 크론잡 `play-idol-test-cycle` 설정 (매일 09:00, 13:00, 17:00, 21:00 KST)

### E2E 테스트 방식 변경 (12:13 KST, 2/16)
- **Reus 지시**: 앞으로 E2E 테스트는 반드시 **브라우저(browser 도구)**로 수행할 것
- 기존: curl/API 직접 호출로 테스트 → **금지**
- 변경: browser 도구로 play-land-dev.semi-colon.space 접속하여 실제 UI 통해 테스트
- 크론잡 프롬프트 업데이트 완료 (curl 금지, browser 도구 필수 명시)

### Supabase 인증 이슈
- 2차 E2E에서 `test@test.com`, `admin@admin.com` 이메일로 Supabase Auth API 직접 요청 시 "Invalid credentials" 발생
- 원인: 이메일 형식 크리덴셜이 잘못되었을 가능성 높음
- TOOLS.md 기록 계정: test / 123123 (일반), admin / ComAdminPass1212 (관리자) — 앱 로그인 UI용
- **Supabase의 정확한 이메일/비밀번호 형식은 아직 미확인** → Reus에게 확인 필요

### 교훈
- API 직접 호출로 테스트하면 실제 UI 동작과 괴리 발생 가능
- 브라우저 테스트가 더 정확하고 신뢰도 높음
- 테스트 결과 보고 시 테스트 방법(브라우저/API)을 명시할 것

## 13:00 - 플레이아이돌 6차 E2E 테스트
- socialLinks 파싱 버그 수정 확인 ✅
- 역할별 마이페이지 메뉴 미노출 발견 → 이슈 #18 생성, PR #19 수정/머지
  - 아이돌 유저: 마이 아이돌, 아이돌 설정 메뉴 추가
  - 어드민: 아이돌 신청 관리 메뉴 추가, 일반 아이돌 신청 숨김
- 배포 대기 중 (GH Actions in_progress)
- 미확인: D-1~D-4 콘텐츠 CRUD, E-3 도네이션

## 크론잡 작업 (12:35~13:25)

### 완료
- PR #14 (socialLinks) 머지 확인
- PR #15 (FE 버그 3건: 팔로우 버튼, 공지사항 undefined, 이미지 fallback) 셀프리뷰 → 머지
- PR #17 (콘텐츠 CRUD FE 구현) 셀프리뷰 → 머지
- 이슈 #16 생성 (콘텐츠 CRUD)
- core-backend#158 생성 (도네이션 BE 이슈)
- 이슈 #12에 콘텐츠 등록 별도 이슈 필요 코멘트

### 6차 E2E 결과
- socialLinks ✅, 공지사항 undefined ✅, 이미지 fallback ✅
- 콘텐츠 CRUD UI ✅ (모달 동작), 콘텐츠 등록 API ❌ (500)
- 팔로우/도네이션 브라우저 hang 발견

### 신규 발견: POST rewrite 이슈
- Next.js rewrite 프록시를 통한 POST 요청이 hang 또는 500 발생
- GET은 정상, POST만 문제 → rewrite + body/cookie 전달 이슈
- 별도 이슈 생성 필요

## 14:30~15:10 - POST 500 근본 원인 분석 및 수정

### 1차 원인: CORS preflight 실패 (FE)
- axios `baseURL`이 `play-backend-dev.semi-colon.space`(외부 오리진)로 설정
- 브라우저 크로스 오리진 POST → CORS preflight(OPTIONS) → Spring 미처리 → 503
- GET은 simple request라 preflight 없이 통과
- **수정**: axios `baseURL`을 빈 문자열로 변경 → Next.js rewrite 프록시 경유
- 이슈 #29, PR #30 머지/배포 완료

### 2차 원인: 백엔드 500 (BE)
- CORS 해결 후 요청이 백엔드 도달하지만 500 반환
- `IdolCashService.processDonation()` 내부 에러
- 가능한 원인: DB 테이블/설정 미등록
- core-backend#158에 상세 분석 코멘트 추가
- **FE에서 더 할 수 있는 건 없음 → BE 수정 필요**

### 교훈
- CORS 문제와 백엔드 에러가 겹쳐 있으면 한번에 원인 파악 어려움
- CORS 해결 후에야 실제 백엔드 에러가 드러남
- curl로 직접 백엔드 호출 시 정상이라고 해서 FE에서도 정상인 건 아님 (CORS)
