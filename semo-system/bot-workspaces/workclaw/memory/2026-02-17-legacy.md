# 2025-07-17

## proj-play-land E2E 테스트 (feat-play-idol 브랜치)

### SSR 페이지 테스트 — 완료 ✅
- 16개 라우트 전부 200 반환
- `/`, `/idol`, `/me`, `/me/idol-apply`, `/me/idol`, `/me/idol/settings`, `/me/information`, `/me/point`, `/me/manage/idol`, `/me/purchase`, `/partners`, `/post`, `/game`, `/join`, `/scams`, `/idol/apply`
- `NODE_OPTIONS="--max-old-space-size=4096"` 필수 (맥미니 OOM 방지)

### 아이돌 신청 E2E 테스트 — 미완료 🔴
- 시나리오: test/123123 로그인 → 아이돌 신청 → admin 로그인 → 승인
- 진행: 로그인 성공, /me/idol-apply 폼 로딩 성공, 폼 입력 완료
- 블로커: "신청하기" 버튼 클릭 후 무반응 (콘솔 에러 없음)
- 원인 추정: standalone 빌드 정적파일 서빙 문제 또는 API 호출 미발생
- 다음: IdolApplyForm 컴포넌트 코드 확인 필요

### 환경 이슈 메모
- `.env`의 `NEXT_PUBLIC_SPRING_API_BASE_URL`이 localhost:8080으로 되어있었음 → dev 서버 URL로 수정
- `feat-play-idol` 브랜치 개발서버 미배포 → localhost에서만 테스트 가능
- next.config.ts에 `typescript.ignoreBuildErrors: true` 임시 추가했다가 되돌림
- `/usr/bin/curl`, `/usr/sbin/lsof` 풀패스 필요 (shell PATH 이슈)
- Slack 스레드: channel `C08P9TDK0UR`, message `1771162978.542959`
- GitHub Issue: #5
