# 2026-03-07 메모리

## 규칙 변경 (SemiClaw 공지)

### 1. 봇 간 Slack 멘션 인계 허용 (12:42)
- **기존**: GitHub 라벨+폴링만
- **변경**: Slack 직접 멘션 인계 허용 + GitHub 방식 병행 가능
- AGENTS.md, SOUL.md 반영 완료

### 2. ReusClaw 인계 절대 금지 (14:23)
- **핵심**: ReusClaw은 Reus 전용 개인 비서, 별개 PC 독립 운영
- **금지 사항**:
  - ReusClaw에게 작업 인계
  - ReusClaw에게 협업 요청
  - "Claude Code" = ReusClaw 매핑
- **올바른 대응**: 코딩 작업은 WorkClaw(나)가 직접 수행
- AGENTS.md, SOUL.md 반영 완료

### 3. Hallucination 재발 방지 — 인프라 정보 검증 의무 (20:39)
- **사건**: InfraClaw가 axoracle.com DNS를 Cloudflare에서 관리한다고 hallucinate
  - 실제: whois.com에서 구매한 도메인
- **Cloudflare 실제 관리 도메인** (이 외는 Cloudflare 아님):
  - jungchipan.net
  - semi-colon.space
  - site-ranking.info
- **재발 방지 원칙** (전 봇 적용):
  1. 인프라 정보(DNS, 도메인, 서버 등)는 **반드시 CLI/콘솔로 실제 확인 후** 발언
  2. 확인 불가능하면 "확인 필요"라고 명시 — **추정을 사실처럼 기술 절대 금지**
  3. 도메인별 DNS 제공자가 다를 수 있음 — **하나를 보고 전체 일반화 금지**

### 4. 주간 봇 메모리 감사 크론 설정 (21:35)
- **스케줄**: 매주 월요일 10:00 KST
- **첫 실행**: 2026-03-09 (다음 월요일)
- **프로세스**:
  1. 자기 메모리/설정 파일 전체 점검 (memory/*.md, SOUL.md, AGENTS.md, TOOLS.md 등)
  2. 중복/충돌 데이터 찾기:
     - 같은 프로젝트 URL이 파일마다 다른 경우
     - 서로 모순되는 규칙/결정이 공존하는 경우
     - 오래된 정보가 새 결정과 충돌하는 경우
  3. #bot-ops 해당 스레드에 발견사항 보고 (무엇이 충돌하고 왜 그런지 분석 포함)
- SemiClaw이 취합해서 Reus에게 원인 분석 + 재발 방지 방안 보고

### 5. AGENTS.md 긴급 업데이트 (21:57)
- **"하겠습니다" 패턴 절대 금지** (중복 섹션 통합, 2026-03-07 날짜 업데이트)
  - 실제 발생 문제: 약속 텍스트만 보내고 tool call 없이 세션 종료 → 파이프라인 중단
  - 검증 기준: tool call 결과 없으면 미완료
- **작업 완료 보고 의무** (신규 섹션 추가):
  - 핸드오프 받으면 👀 + "착수합니다" 보고
  - 완료 시 결과 보고 필수
  - 30분 초과 시 중간 진행 보고
- **`bot:needs-design` 라벨 신설**:
  - PlanClaw: 디자인 필요한 이슈에 라벨 부착
  - DesignClaw: 폴링으로 작업 감지

### 6. Cron Job delivery.to 규칙 (22:54)
- **사건**: InfraClaw이 cron job `delivery.to`에 채널 이름 사용 → 보고 누락
- **규칙**: `delivery.to`는 **채널 ID만** 허용
  - ❌ `#proj-axoracle` (채널 이름)
  - ✅ `C0AE4N0LSKV` (채널 ID)
- **채널 ID 확인 방법**:
  1. 자기 메모리 파일에서 채널 매핑 확인
  2. 모르면 SemiClaw에게 문의
  3. Slack 채널 상세보기 하단에서 확인

## 2026-03-08 (익일 새벽 공지)

### 7. 할루시네이션 재발 방지 규칙 (01:57)
- **사건**: InfraClaw가 AXOracle이 "아직 Vercel"이라고 답변 → 실제로는 어제 OCI로 이전 완료 + Vercel 삭제됨
  - GrowthClaw, WorkClaw까지 잘못된 전제 수용 → 봇 3개 헛바퀴
- **근본 원인**:
  1. 작업 완료 후 MEMORY.md 미업데이트 → 다음 세션에서 오래된 정보로 판단
  2. 메모리만 믿고 실제 상태 확인 안 함
  3. 봇 간 인계 시 전달받은 전제를 독립 검증 없이 수용
- **재발 방지 규칙** (전 봇 필수):
  1. **중대 작업 후 메모리 즉시 업데이트**
     - 인프라 변경, 배포 환경 변경, 프로젝트 구조 변경 등 완료 시 → 해당 세션에서 MEMORY.md 반드시 업데이트
     - "다음에 하지" ❌ → "지금 바로" ✅
  2. **상태 질문엔 실제 확인 우선 (Trust but Verify)**
     - 배포/인프라/서비스 상태 질문 → 메모리 의존 ❌
     - 실제 확인 먼저: `curl`, `kubectl`, `gh api`, `git log` 등
     - 메모리는 참고용, 실제 상태가 source of truth
  3. **봇 간 인계 시 독립 검증**
     - 다른 봇이 전달한 전제("Vercel 배포 중" 등)를 그대로 신뢰 ❌
     - 본인이 직접 확인 가능한 수단으로 검증 후 작업 착수
