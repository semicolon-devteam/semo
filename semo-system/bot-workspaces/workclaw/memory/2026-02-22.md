# 2026-02-22

## axoracle PR #53 리뷰 완료 → E2E 테스트 시도 중 기존 버그 발견

### 타임라인
- 03:06 — ReviewClaw이 PR #53 리뷰 완료 + 머지
- 03:08 — E2E 테스트 시도 → 로컬에서 런타임 에러 발견
- 03:10 — 조사 시작: main + feature 브랜치 모두 동일 에러
- 03:15 — .next 캐시 삭제, 프로덕션 빌드 시도 → 빌드 성공, 런타임 실패
- 03:20 — 이전 커밋(8bae836) 체크아웃 → 동일 에러 재현 (기존 버그 확정)
- 03:25 — SemiClaw에 보고 + 프로젝트 정보 요청

### 발견된 버그
- **에러**: `Cannot read properties of undefined (reading 'call')`
- **위치**: `src/app/layout.tsx (102:9) @ RootLayout`
- **컴포넌트**: `<OrganizationJsonLd />`
- **영향 범위**: main 브랜치 + 여러 이전 커밋
- **빌드**: ✅ 성공 (프로덕션 빌드 정상)
- **런타임**: ❌ 실패 (브라우저에서 앱 로드 불가)

### 조사 결과
1. PR #53과는 무관 (SurveyOverlay 변경사항과 별개)
2. OrganizationJsonLd 주석처리해도 해결 안됨 → 다른 원인
3. 8bae836 커밋(2주 전)에서도 동일 → 오래된 버그

### 추가 조사 (03:30 ~)
- ✅ 프로덕션(axoracle.com) 확인 → **정상 작동** (에러 없음)
- ✅ node_modules 삭제 + 재설치 → 해결 안됨
- ✅ git checkout으로 로컬 변경사항 초기화 → 해결 안됨
- ❌ 프로덕션 빌드 로컬 실행 → 동일 에러

### 결론
- **로컬 환경 이슈** (코드 자체는 문제없음)
- PR #53과 무관
- 프로덕션 정상 작동 = 긴급하지 않음
- E2E 테스트는 실제 프로덕션에서 진행 가능

### 다음 단계
- [ ] PR #53 프로덕션 E2E 테스트 (axoracle.com)
- [ ] 로컬 dev 서버 에러 → 별도 이슈 (낮은 우선순위)
- [ ] axoracle 배포 파이프라인 확인 필요

### 메모
- TOOLS.md, memory에 axoracle 프로젝트 정보 없음
- SemiClaw rate limit 중, 직접 조사 진행
- ReviewClaw과 협의 중
