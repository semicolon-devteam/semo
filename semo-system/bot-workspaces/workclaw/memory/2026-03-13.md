# 2026-03-13 WorkClaw Daily Log

## 워크플로우 업데이트: bot:changes-requested 라벨 전환 (16:34)

### 변경 사항
SemiClaw 공지:
- 새 라벨 `bot:changes-requested` 추가 (기존 `bot:request-changes` 대체)
- ReviewClaw: PR에 changes requested 시 → `bot:changes-requested` 라벨 부착
- WorkClaw: `label:bot:changes-requested` 폴링 추가 → 피드백 반영 후 라벨 제거
- **`bot:blocked`는 사람 개입 필요 시에만 사용**

### 구현 (16:35)

#### 1. 크론 잡 업데이트
- Job ID: `084572ca-ac3a-4e08-aab2-3dc6282cb7ae`
- 기존 이름: `github-polling-request-changes`
- 새 이름: `github-polling-changes-requested`
- 폴링 쿼리: `label:bot:changes-requested -label:bot:in-progress -label:bot:blocked`
- 주기: 30분

#### 2. Memory 파일 업데이트
- `memory/github-rules.md`:
  - "라벨 전환 후 인계 방식" 섹션 업데이트
  - "bot:changes-requested vs bot:blocked" 섹션 추가
  - "WorkClaw 폴링 쿼리" 업데이트: `is:pr is:open review:changes_requested` → `label:bot:changes-requested`
- `memory/infra.md`:
  - GitHub 폴링 섹션 재구성
  - bot:changes-requested 폴링 정보 추가

### 플로우
1. ReviewClaw: PR 리뷰 → `Request changes` + `bot:changes-requested` 라벨 부착
2. WorkClaw: 30분 폴링으로 감지 → 피드백 반영 → push
3. WorkClaw: `bot:changes-requested` 제거 + `bot:needs-review` 재부착
4. ReviewClaw: 재리뷰

### Lessons Learned
- 라벨 이름 변경 시 크론 잡, 폴링 쿼리, 문서 모두 일괄 업데이트 필요
- `bot:blocked`는 기술적 결정/외부 의존성/불명확한 요구사항 등 사람 개입 필요 시에만 사용
- `bot:changes-requested`는 자동화 가능한 코드 개선 요청에 사용

### Time Tracking
- Start: 16:34 (SemiClaw 공지)
- End: 16:36 (문서 업데이트 완료)
- Duration: ~2 minutes

---
