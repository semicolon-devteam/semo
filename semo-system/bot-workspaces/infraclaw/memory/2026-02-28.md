# 2026-02-28 - InfraClaw Daily Log

## kubectl 접근 불가 문제 진단 & 해결 시도 (19:40-20:13)

### 문제 발견
- **Roki 요청**: kubectl 접근 가능 여부 보고
- **증상**: `kubectl get nodes` → `Client.Timeout exceeded while awaiting headers`
- **진단 결과**: OKE API 서버 엔드포인트 (`https://131.186.18.78:6443`)에 네트워크 접근 불가
- **근본 원인**: 현재 Mac Mini IP (`222.106.188.17`)가 OKE API Security List allowlist에 없음

### 시도한 해결 방법
1. **Terraform으로 IP 추가 시도**:
   - `core-terraform` 레포 확인 (`/Users/reus/Desktop/Sources/semicolon/core-terraform`)
   - `environments/dev/oci/terraform.tfvars` 수정: `allowed_api_cidrs`에 `222.106.188.17/32` 추가
   - Garden 승인 받고 `terraform apply` 실행

2. **실패 원인**:
   - **Dynamic Group 중복 에러**: `dg-oke-cluster-semi-dev`, `dg-oke-nodes-semi-dev` 이미 존재
   - **근본 문제**: 기존 OKE 인프라가 이미 배포되어 있는데, Terraform state가 이를 추적하지 않음
   - Terraform이 "첫 생성"으로 인식하여 기존 리소스와 충돌

### 발견한 인프라 정보
- **OKE 클러스터 OCID**: `ocid1.cluster.oc1.ap-seoul-1.aaaaaaaafukrunslwu3gszj53t3fa5jgsixjfl4ps3gl2w64ycorhq23ukgq`
- **클러스터 이름**: `oke-semi-dev`
- **Compartment**: `semi-dev` (ocid1.compartment.oc1..aaaaaaaayymloiuiaxdghgfzxf7ntkunui6g4yomljg4mwcas75kms4dr6da)
- **API 엔드포인트**: `https://131.186.18.78:6443`
- **현재 Mac Mini IP**: `222.106.188.17`

### Garden에게 제시한 해결 옵션
1. **옵션 1 (빠름, 권장)**: OCI 콘솔에서 수동으로 Security List 수정
   - VCN → `sl-oke-api-dev` → Ingress Rule 추가: `222.106.188.17/32` → TCP 6443
2. **옵션 2 (느림)**: Terraform으로 기존 인프라 전체 Import 후 관리
   - 기존 리소스 50개+ import 필요 (1-2시간 소요)

### 교훈
- **Terraform state 불일치**: 기존 인프라가 Terraform 외부에서 생성되었거나, state 파일이 없는 상태
- **변경 통제 위반 회피**: Terraform apply 실패로 인프라 임의 변경 방지 (Garden 규칙 준수)
- **앞으로**: 인프라 변경 전 반드시 Terraform state 확인 필요

## 진행 상태
- ⏸️ **대기 중**: Garden의 해결 옵션 선택 대기 (20:13 기준)

---

## Terraform Apply 실패 상세 분석 (세션 종료 전)

### 실패한 명령
```bash
terraform apply -auto-approve
```

### 발생한 에러 (3가지)
1. **DNS Resolver 필수 인자 누락**
   - 모듈에서 DNS resolver 설정 필요하나 인자 미제공
   
2. **KMS 키 생성 실패**
   - DNS lookup error로 KMS vault 생성 실패
   
3. **Dynamic Group 중복 생성**
   - `dg-oke-cluster-semi-dev`: 이미 OCI에 존재
   - `dg-oke-nodes-semi-dev`: 이미 OCI에 존재

### 근본 원인
- **Terraform state 미동기**: 기존 OKE 인프라가 이미 배포되어 있지만 state 파일 없음
- **첫 배포로 인식**: Terraform이 58개 리소스를 "새로 생성"하려고 시도 → 기존 리소스와 충돌

### 작업 내역
- **수정된 파일**: `/Users/reus/Desktop/Sources/semicolon/core-terraform/environments/dev/oci/terraform.tfvars`
- **추가한 IP**: `222.106.188.17/32` (Mac Mini 현재 IP)
- **작업 디렉토리**: `/Users/reus/Desktop/Sources/semicolon/core-terraform/environments/dev/oci`

### 다음 세션 시 해야 할 일
1. Garden의 옵션 선택 확인 (수동 vs Terraform import)
2. 옵션 2 선택 시: Terraform import 작업 진행
3. kubectl 접근 복구 확인: `kubectl get nodes`
4. 성공 시 #platform-land에 Roki + Garden 보고
