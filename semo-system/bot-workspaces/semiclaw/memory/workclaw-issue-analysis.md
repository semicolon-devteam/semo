# WorkClaw 작업 미완료 분석 (2026-02-26)

## 이슈 #167 상황
- **증상**: GitHub 이슈 코멘트에 "작업 착수" 로그만 남고 실제 PR 없음
- **브랜치**: feat/167-fix-fouc 생성됨 (커밋 없음)
- **스태시**: 작업 내용 2개 스태시에 남아있음

## 근본 원인

### 1. Git 워크플로우 실패
- CLAUDE.md 병합 충돌
- 로컬 변경사항 있는 상태에서 브랜치 전환 실패
- Edit 도구 exact match 실패 (반복)

### 2. 타임아웃 문제
- 설정: 600초 (크론 payload)
- 실제: 60초 gateway timeout 발생
- 원인: 작업 복잡도 + 에러 복구 시도로 시간 초과

### 3. 코딩 에이전트 스킬 미사용
- WorkClaw가 직접 git 명령어로 작업 시도
- coding-agent 스킬 (PTY 기반) 사용 안 함
- 결과: 에러 핸들링 부족, 충돌 해결 실패

### 4. 에러 핸들링 부족
- 실패 시 롤백 로직 없음
- 스태시 정리 없음
- 이슈 상태 업데이트 불완전

## 권장 해결책

### 즉시 조치
1. **WorkClaw 크론 설정 수정**
   - coding-agent 스킬 사용하도록 프롬프트 수정
   - 타임아웃 → 900초 (15분)로 증가
   - 폴링 간격 → 10분으로 증가 (중복 실행 방지)

2. **에러 핸들링 추가**
   ```
   - 작업 실패 시: bot:blocked 라벨 + 코멘트
   - 스태시 정리 로직
   - 브랜치 정리 로직 (커밋 없으면 삭제)
   ```

3. **작업 단순화**
   - 한 번에 1개 이슈만 처리 (이미 적용됨)
   - 복잡한 작업은 bot:blocked로 에스컬레이션

### 장기 개선
1. **coding-agent 통합**
   - WorkClaw TOOLS.md에 coding-agent 사용 가이드 추가
   - 프로젝트별 경로 설정 명시

2. **모니터링 강화**
   - 폴링 상태를 memory/polling-state.json에 기록
   - 실패 이력 트래킹

3. **프롬프트 개선**
   - "코딩 게이트 준수" → "coding-agent 스킬 사용" 명시
   - Git 충돌 시 자동 에스컬레이션 로직

## 후속 조치 (Reus 확인 필요)
- [ ] WorkClaw 크론 설정 업데이트
- [ ] WorkClaw TOOLS.md에 프로젝트 경로 추가
- [ ] 이슈 #167 스태시 내용 확인 및 수동 처리 여부 결정
- [ ] 다른 봇들도 동일 패턴 검토
