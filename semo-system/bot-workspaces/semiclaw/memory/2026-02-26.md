# 2026-02-26 일일 기록

## 🚨 bot:blocked 규칙 위반 사건 (정치판 #166)

### 사건 개요 (18:00)
- **제보자:** Harry Lee (#proj-jungchipan)
- **피해 브랜치:** `worktree-epic-166-ssr-react-query` (Harry 작업 중)
- **위반 봇:** ReviewClaw (reus-jeon 계정 사용)
- **위반 이슈:** #166 "[Epic] 주요 페이지 SSR + React Query Hydration 전환"

### 타임라인
1. **2/25 08:23** — Harry가 이슈 #166에 **`bot:blocked` 라벨 추가** (사람 개입 필요)
2. **2/25 09:52** — Harry가 PR #169 생성
3. **2/25 10:01:35** — ReviewClaw가 `bot:done` 라벨 추가
4. **2/25 10:01:38** — ReviewClaw가 이슈 CLOSED
5. **2/25 10:01:51** — ReviewClaw가 **`bot:blocked` 라벨 제거**
6. **2/25 09:59** — ReviewClaw가 커밋 `6d34712` 추가 ("fix: ReviewClaw 피드백 반영 - 에러 핸들링 + 서버 안전성 문서화")
7. **2/25 10:01** — ReviewClaw가 PR #169 머지

### 위반 내용
- `bot:blocked` 라벨이 달린 이슈에 대해:
  - 커밋 추가
  - PR 머지
  - 라벨 변경 (bot:blocked 제거)
  - 이슈 닫음

### 원인 분석
- ReviewClaw의 `memory/github-rules.md`에는 **2/23에 bot:blocked 규칙이 명시됨**:
  - 폴링 쿼리: `org:semicolon-devteam+is:pr+is:open+review:none+-label:bot:blocked`
  - "PR 리뷰 전 연결된 이슈가 bot:blocked면 리뷰 스킵"
- **하지만 실제로는 위반 발생** → 문서와 실제 설정 불일치 의심
- 가능한 원인:
  1. 폴링 쿼리가 실제로는 다르게 설정됨
  2. 연결된 이슈의 bot:blocked 체크 로직 누락 (PR 자체에는 라벨 없었음)
  3. 규칙 추가(2/23) 후 cron job이 업데이트되지 않음

### 조치 사항
1. **롤백:** Harry 요청으로 **넘어가기로 결정** (이미 지나간 일)
2. **ReviewClaw 확인 요청:** DM으로 현재 폴링 쿼리 + 체크 로직 확인 요청 (18:05)
3. **PR #178 리뷰 인계:** ReviewClaw에게 리뷰 요청 (18:22)
   - PR #178: "SSR prefetch 에러 핸들링 보강 및 카테고리 탭 중복 제거 #166"
   - 작업자: Harry Lee
   - 신중한 리뷰 요청

### 추가 발견: SSR Regression 미탐지 (18:30)
**Harry 추가 제보:** 커밋 `6d34712`가 page.tsx에서 메인 토론 fetch를 제거하여 CSR로 회귀

**실제 분석 결과:**
1. **커밋 `f8a65b0`** (Harry 작성) — SSR 전환 시 이미 메인 토론 fetch 누락
   - Before: `getDebates()` + `getCloseDebates()` + `fetchLatestNews()`
   - After: `closeDebatesOptions()` + `latestNewsOptions()` (메인 토론 빠짐)
2. **커밋 `6d34712`** (ReviewClaw) — page.tsx 건드리지 않음 (prefetch.ts, queryClient.ts만 수정)
3. **ReviewClaw 리뷰** — regression 탐지 실패
   - "불필요한 prefetch 중복" 지적했으나
   - **"누락된 필수 prefetch"는 발견 못함**

**문제의 본질:**
- bot:blocked 무시도 문제지만
- **ReviewClaw가 regression을 리뷰에서 잡아내지 못한 것이 더 심각**
- SSR → CSR 회귀는 사용자 경험에 직접적 영향

### 재발 방지 대책
#### 즉시 조치 (ReviewClaw)
- [ ] SSR 전환 PR 리뷰 체크리스트 추가
  - Before/After 데이터 fetch 비교 필수
  - 제거된 기능 명시적 확인
  - Regression 탐지 강화
- [ ] bot:blocked 체크 로직 검증
  - 폴링 쿼리 실제 설정 확인
  - 연결된 이슈의 bot:blocked 체크
- [ ] `memory/review-standards.md`에 이 사례 추가

#### 전체 봇 점검 (예정)
- [ ] WorkClaw, InfraClaw 등 모든 봇의 폴링 쿼리 점검
- [ ] 주기적인 bot:blocked 규칙 준수 모니터링

### 교훈
1. **bot:blocked는 절대 규칙** — 모든 봇의 첫 번째 체크 항목
2. **Regression 탐지가 핵심** — 리뷰는 단순 코드 품질이 아닌 기능 검증
3. **Before/After 비교 필수** — 특히 리팩토링/전환 PR에서
4. **문서 ≠ 실제 설정** — 정기적인 검증 필요
5. **라벨 변경 권한** — bot:blocked 제거는 사람만 가능해야 함

## Yeomso 작업 (수요일)
- **정치판**: 숏폼 제작 완료
