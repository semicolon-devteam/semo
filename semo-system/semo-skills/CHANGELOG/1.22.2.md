# semo-skills 1.22.2

> 2025-01-05

## Changed

- `create-feedback-issue` 스킬에 `feedback` 라벨 필수 추가
  - 모든 피드백 이슈에 `feedback` 라벨이 자동으로 포함됨
  - 버그: `feedback`, `bug`, `{package}`
  - 개선 제안: `feedback`, `enhancement`, `{package}`

## Added

- `semicolon-devteam/semo` 레포지토리에 `feedback` 라벨 생성
  - Description: "User feedback for SEMO package"
  - Color: #0E8A16 (green)
