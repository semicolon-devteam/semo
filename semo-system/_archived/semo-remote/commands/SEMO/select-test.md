# /SEMO:select-test

SEMO Remote 옵션 선택 인터랙션 테스트를 위한 커맨드입니다.

## 용도

- SEMO Remote client-app 연동 테스트
- Claude Code 세션의 옵션 선택 인터랙션 검증
- Mobile PWA에서 선택지 응답 플로우 테스트

## 동작

이 커맨드가 호출되면:

1. `AskUserQuestion` 도구를 사용하여 선택지를 표시합니다
2. SEMO Remote client-app에서 옵션을 선택할 수 있습니다
3. 선택 결과를 표시합니다

## 응답

```markdown
[SEMO] Remote Test: select-test

🎯 옵션 선택 인터랙션 테스트를 시작합니다.

**테스트 시나리오**: 다중 선택지 응답
- 예상 동작: Claude Code 선택 UI 표시
- SEMO Remote에서 응답 가능

아래에서 선택 요청이 표시됩니다...
```

## 실행

`AskUserQuestion` 도구를 호출하여 다음 질문을 표시합니다:

**질문**: "SEMO Remote 테스트입니다. 원하는 옵션을 선택하세요."

**선택지**:
1. 옵션 A - 첫 번째 테스트 옵션
2. 옵션 B - 두 번째 테스트 옵션
3. 옵션 C - 세 번째 테스트 옵션

## 완료 메시지

```markdown
[SEMO] Remote Test: select-test 완료 ✅

| 항목 | 결과 |
|------|------|
| 선택 요청 | ✅ 표시됨 |
| 사용자 선택 | {선택된 옵션} |
| 응답 수신 | ✅ 완료 |

선택된 값: **{옵션 내용}**
```
